# 원정 데일리 미션 — 설계 (에픽 #408, W1 게이트 산출물)

> hero 요구(2026-08-02, 메가 에픽 요구 11): *원정경기 데일리 미션 — 하루마다 초기화되는 미션 **2개**, 달성 시 보상.
> 미션은 **랜덤**, 보상은 미션에 따라 결정, **보상은 전부 다이아**. 예: "원정 2연승 = 200다이아" · "한 경기 3골 달성" ·
> "총 3승 달성". **하스스톤 데일리 미션 참고.** 처음 미션 2개가 주어지고 **각각 한 번씩 리롤** 가능.*
>
> 이 문서 = W1 게이트 산출물. **hero 컨펌 완료(2026-08-02, §7)** — 확정 사항은 재질문하지 않는다.
> 읽는 사람이 이 에픽을 처음 본다는 전제로 썼다 — §1 이 "왜 지금 이걸 만드나", §7 이 "무엇이 확정됐나".

---

## 0. 한 장 요약 (hero 확정)

| 항목 | 확정 |
|---|---|
| 미션 수 | 하루 **2개** — **14종에서 균등 추첨, 중복만 금지**(티어 고정 슬롯 없음) |
| 미션 풀 | **14종**(쉬움 5 · 보통 5 · 어려움 4), **전부 원정 경기로만** 판정 |
| 보상 | 티어별 **쉬움 100 Z · 보통 200 Z · 어려움 300 Z** (전부 다이아) |
| 하루 보상 | **200 ~ 600 Z**, 기대 **386 Z** → 10연(3,000 Z) 한 판에 약 8일 |
| 리셋 | **KST 자정** — 기존 원정 일일 한도·컨디션·리그 트랙과 같은 경계 |
| 리롤 | 미션당 1회, **전체 풀에서 재추첨**(보유 중인 것과 중복 금지), 진행도 초기화 |
| 이월 | **미완료 미션은 이월 없음**(자정에 교체). 단 **달성했는데 안 받은 보상은 기한 없이 남는다** |
| 신규 유저 | 첫 조회 시점에 그날 미션 생성(별도 지급 시점·리셋 잡 없음) |
| 화면 | **원정 페이지 안 섹션** + 홈에는 "받을 보상 N건" 한 줄 |

---

## 1. 왜 지금 이걸 만드나 — 다이아 수도꼭지가 실제로 잠겨 있다

이건 "보상 하나 더 얹기"가 아니다. **이 게임의 첫 반복 다이아 수급원**을 만드는 일이다.

라이브 스냅샷(`pre-v315-20260802T070503Z.db`, 읽기전용 사본, V36 시점) 젬 원장 전수:

| reason | 건수 | 유저 | 획득 | 사용 |
|---|---|---|---|---|
| `initial_gems` (가입 지급) | 191 | 191 | 1,392,000 | |
| `admin_grant` (수동 일괄) | 139 | 139 | 834,000 | |
| `gacha_ten` · `gacha_single` | 45 | 16 | | 126,900 |
| **`league_daily_gem`** (리그 매판 트랙 #368) | 7 | 4 | **210** | |
| `mail_claim` | 2 | 2 | 310 | |
| **`league_gem_reward`** (리그 시즌 종료) | **0** | — | **0** | |

- **게임플레이로 벌린 다이아 총합 = 520 Z (5명).** 무상 지급은 2,226,000 Z.
- 시즌 종료 보상(3,000~9,000 Z)은 **한 번도 지급된 적이 없다** — 리그 시즌 7개가 전부 진행 중(최대 9/18 소화).
- 리그 매판 트랙은 하루 18칸(이론 최대 1,080 Z)인데, 활성 유저가 실제로 치는 경기가 **하루 1~4.5판**이라
  관측 실사용이 **1~3칸(30~90 Z/일)** 이다. 18칸은 실플레이의 5배라 사실상 안 닿는다.
- 결과: **7명이 잔액 정확히 0 Z, 6명이 가입분 12,000 Z 를 전액 소진.** 이들은 리그 시즌이 끝나기 전까지
  **다이아를 벌 방법이 문자 그대로 없다.**

### 가격 앵커 (보상표의 기준자)

| | 값 |
|---|---|
| 10연 뽑기(11장) | **3,000 Z** |
| 단차 뽑기 | 300 Z |
| 캐시 다이스 | 10 Z |
| 가입 지급 | **12,000 Z**(라이브 override. 리포 `economy.v3.json` 은 6,000 — 인용 시 주의) |
| 리그 시즌 완주 | 3,000 Z (+순위 보너스 1위 6,000 / 2위 3,000 / 3위 1,000) |
| 리그 일일 트랙 | 소량 30 · 대량 300, 하루 이론 최대 1,080 / **실측 30~90** |

---

## 2. 레퍼런스 — 하스스톤은 무엇을 하고 있나

hero 가 지정한 레퍼런스. 우리 요구와 다른 지점을 굵게 표시했다.

| | 하스스톤 | 클래시 로얄 | 마블 스냅 | **HMB (요구)** |
|---|---|---|---|---|
| 동시 보유 | 3개(꽉 차면 그날 지급분 소멸) | 3개 | 다수 | **2개** |
| 리롤 | **하루 총 1회** | 하루 1회 무료, 이후 10젬 | — | **미션당 1회 = 하루 2회** |
| 리셋 | 서버 현지 자정 | 일 단위 | 일 단위 | KST 자정(제안) |
| 보상 티어 | 800~900 / 1,000 / **1,500**(프리미엄) | 상자(변동) | **50 / 100**(정확히 2배) | 티어별 다이아 |
| 티어 비율 | 기본 대비 **1.7~1.9배** | — | **2배** | **1 : 2 : 3**(제안) |
| 풀 크기 | 65종 | — | — | 14종(제안) |

**읽어낸 것 3가지**

1. **"판수형(쉬움) < 승수형(보통) < 특수조건(프리미엄)"** 3단 구조가 공통이다.
2. 티어 간 보상 배율은 **1.7~2배**로 수렴한다 — 난이도 대비 보상이 지나치게 가파르면 쉬운 미션이 무의미해지고,
   평평하면 어려운 미션을 할 이유가 없다.
3. 하스스톤은 리롤이 **하루 총 1회**인데 우리 요구는 **미션당 1회**다. 더 관대하다 — 이게 문제가 되는지는 §6.2 에서 다룬다.

---

## 3. 실측 — 미션 난이도를 추측하지 않기 위해

미션 임계값을 감으로 잡으면 "아무도 못 깨는 미션"이나 "그냥 주는 미션"이 된다. 라이브 경기 데이터로 실측했다.

### ⚠️ 표본 경고 — 먼저 읽어야 한다

| | 값 |
|---|---|
| FINISHED 매치 전체 | 74 |
| **그 중 원정** | **16** (유저 7명, user-day 10개) |
| 기간 | 2026-07-21 ~ 08-02 |

**원정 16경기 중 11경기(69%)가 단 2명에게서 나왔다.** 원정 백분율의 95% 신뢰구간은 ±20~25%p 다.
아래 수치는 **자릿수 감**이지 정밀값이 아니다. 그래서 이 설계는 실측을 **티어 분류에만** 쓰고,
**금액은 실측이 아니라 §1 의 가격 앵커에서 끌어온다**(표본이 흔들려도 보상표가 흔들리지 않도록).

데이터 무결성 자체는 3중 교차검증을 통과했다 — 파생 승패 74/74 = 저장된 `result`, 원정 16/16 = `away_reports` 미러,
매치로그 골 합계 74/74 = 스코어 컬럼. **수치는 정확하고, 표본 크기만 문제다.**

### 3.1 원정 경기 1판당 빈도

| 지표 | 원정 실측(n=16) | 비고 |
|---|---|---|
| 승 / 무 / 패 | **43.8% / 0% / 56.3%** | **무승부 0건** |
| 팀 득점 평균 | 1.63골 | 실점 1.88 |
| 한 경기 2골 이상 | **37.5%** | |
| 한 경기 3골 이상 | **31.2%** | |
| 한 경기 4골 이상 | 12.5% | |
| 무실점 | **12.5%** | 원정 상대(고스트)가 잘 넣는다 |
| 승 + 2골차 이상 | 31.2% | |
| 승 + 3골차 이상 | 6.2% (1/16) | 사실상 표본 없음 |

### 3.2 🔴 하루에 치는 원정 판수 — 이게 가장 센 제약이다

원정 user-day 10개 전수:

| 하루 원정 판수 | 도달 user-day |
|---|---|
| ≥1판 | 10 / 10 (**100%**) |
| **≥2판** | **3 / 10 (30%)** |
| ≥3판 | 2 / 10 (20%) |
| ≥4판 | 1 / 10 (10%) |
| ≥5판 | **0건** |

중앙값 = **하루 1판**. 즉 **"원정 2승"·"2연승"류는 오늘의 전형적인 유저에게 물리적으로 불가능하다.**

⚠️ 다만 이건 **인과가 아니라 현상**이다. 지금 원정을 더 칠 이유가 없어서 안 치는 것이고,
원정 일일 한도는 **10판**이라 여유는 충분하다. 데일리 미션이 바로 그 이유를 만드는 장치다.

그래도 **"오늘 절대 못 깨는 미션 2개"는 리텐션에 마이너스**다. 세션은 이걸 배분 규칙으로 막자고
제안했으나(쉬움 1개 고정) **hero 가 기각**했다(§7 Q2) — 변동폭이 없으면 뽑는 재미가 없다는 이유.
⇒ **이 위험은 리롤이 흡수한다**(§5.1 · §6.2). 배분과 리롤은 한 쌍이다.

### 3.3 🔴 "한 선수 N골" 미션은 성립하지 않는다

매치로그의 골 이벤트를 (경기·팀·선수)로 집계한 결과:

| | 원정 | 전체 |
|---|---|---|
| P(한 선수 2골 \| 팀 2골 이상) | **6/6 = 100%** | 35/36 = **97%** |
| P(한 선수 3골 \| 팀 3골 이상) | **5/5 = 100%** | 20/21 = **95%** |

팀이 4골 넣은 5경기는 **전부 한 선수가 4골을 넣었고**, 7골 넣은 2경기도 **한 선수가 7골**이었다.
엔진이 스트라이커 한 명에게 득점을 몰아준다 → **"한 선수 2골" 미션 ≈ "팀 2골" 미션**이라 별도 티어로 구분되지 않는다.
⇒ **개인 득점 미션은 풀에서 제외**한다(엔진의 득점 분산이 개선되면 그때 다시 본다 — 엔진 트랙 #407 소관).

---

## 4. 미션 풀 (14종)

전부 **원정 경기**로 판정하고, **하루(KST) 안에서만** 집계한다. 티어는 **달성에 필요한 기대 원정 판수**로 나눴다.

### 쉬움 (T1) — 1판이면 닿는다 · **100 Z**

| id | 문구 | 판정 | 1판 달성률 |
|---|---|---|---|
| `away_play_1` | 원정 경기를 1회 치른다 | 완료된 원정 매치 1건 | 100% |
| `away_win_1` | 원정에서 1승 | `result=WIN` | 44% |
| `away_goals_2` | 원정 한 경기에서 2골 | `userGoals >= 2` | 38% |
| `away_first_goal` | 원정 경기에서 선제골을 넣는다 | 매치로그 첫 `goal` 이벤트가 우리 팀 | ~40% |
| `away_play_2` | 원정 경기를 2회 치른다 | 완료 2건 | (2판) |

### 보통 (T2) — 2~3판 · **200 Z**

| id | 문구 | 판정 | 달성률(2판 / 3판) |
|---|---|---|---|
| `away_win_2` | 원정에서 2승 | 하루 누적 승 2 | 19% / 41% |
| **`away_streak_2`** | **원정 2연승** ← hero 예시 | 하루 안 연속 승 2 | 19% / 30% |
| `away_goals_3` | 원정 한 경기에서 3골 | `userGoals >= 3` | 52% / 67% |
| `away_clean_win` | 원정에서 무실점 승리 | `WIN && oppGoals == 0` | 23% / 33% |
| `away_play_3` | 원정 경기를 3회 치른다 | 완료 3건 | — / 100% |

### 어려움 (T3) — 3~4판 · **300 Z**

| id | 문구 | 판정 | 달성률(3판 / 4판) |
|---|---|---|---|
| `away_win_3` | 원정에서 3승 | 하루 누적 승 3 | 8.5% / 23% |
| `away_streak_3` | 원정 3연승 | 하루 안 연속 승 3 | 8.5% / 14% |
| `away_margin_3` | 3골차 이상으로 승리 | `WIN && userGoals-oppGoals >= 3` | 17% / 22% |
| `away_goals_4` | 원정 한 경기에서 4골 | `userGoals >= 4` | 33% / 41% |

> 달성률은 원정 승률 44%·§3.1 빈도를 이항 모델에 넣은 **추정치**다(표본이 작아 실측 직접 인용 불가).

### 풀에서 뺀 것과 이유 — 되살리려면 근거가 필요하다

| 뺀 것 | 이유 |
|---|---|
| 어시스트 미션 | **엔진에 `assist` 이벤트 타입이 없다**(`MatchEventType` 전수 확인). 만들려면 엔진 계약 변경 |
| 한 선수 2골 / 해트트릭 | §3.3 — 팀 득점과 **97% 같은 축**이라 티어가 구분되지 않는다 |
| 원정 무승부 | 원정 16전 **무승부 0건**. 근거 없음 |
| 한 경기 5골 이상 | 관측 1/16. "가끔 되는 것"이 아니라 "거의 안 되는 것" |
| 세이브·태클 등 세부 스탯 | 매치로그에는 있으나 **화면 어디에도 표시되지 않아** 유저가 진행도를 이해할 수 없다 |
| 연속 출석(N일) | 데일리 미션이 아니라 **출석 보상**이라는 다른 축. 섞으면 둘 다 흐려진다 |

---

## 5. 보상표

### 5.1 배분 규칙 — **14종 균등 추첨, 중복만 금지** (hero 확정)

```
슬롯 A, B = 14종에서 균등 추첨(각 1/14), 두 슬롯이 같은 미션이 되지 않게만 배제
```

| | 확률 |
|---|---|
| 쉬운(T1) 미션이 **하나도 없는 날** | **40%** |
| **둘 다 어려운(T3)** 날 | 7% |
| 하루 기대 보상(전부 달성 시) | **386 Z** |
| 하루 보상 범위 | **200 ~ 600 Z** |

⚠️ **§3.2 의 위험을 이 규칙은 흡수하지 않는다.** 중앙값 유저는 하루 1판을 치는데, 40% 의 날은 1판짜리 미션이
하나도 없다. **그래서 리롤이 유일한 구제 장치다**(§6.2) — 이 두 결정은 묶여 있고, 한쪽만 바꾸면 다른 쪽이 무너진다.
리롤을 좁히거나 없애려면 배분 규칙을 같이 다시 봐야 한다.

> 검토했다가 hero 가 기각한 대안 = **쉬움 1 + 보통·어려움 1 고정**(하루 300~400 으로 고정, 못 깨는 날 없음).
> 변동폭이 사라져 뽑기의 재미가 없다는 것이 기각 사유. 되살릴 근거가 생기면 리롤 정책과 함께 재검토한다.

### 5.2 금액 — 검토한 3안과 확정 (hero 확정 = B)

| | 쉬움 | 보통 | 어려움 | 하루 범위 | 하루 기대 | **10연(3,000 Z)까지** | 30일 기대 |
|---|---|---|---|---|---|---|---|
| A 보수 | 50 | 100 | 150 | 100~300 | 193 | **16일** | 5,790 |
| **B 확정** | **100** | **200** | **300** | **200~600** | **386** | **8일** | **11,580** |
| C 관대 | 150 | 300 | 500 | 300~1,000 | 621 | **5일** | 18,630 |

**B 를 고른 근거 4가지**

1. **hero 의 앵커와 정확히 맞는다.** 요구에 적힌 *"원정 2연승 = 200다이아"* 에서 2연승은 T2 다 → T2 = 200.
   나머지는 업계 수렴 배율(1:2~1:3)로 채운 것이라 앵커를 흔들지 않는다.
2. **한 달 꼬박 하면 가입 지급 1회분.** 30일 기대 11,580 Z ≈ 라이브 가입 지급(12,000 Z).
   "매일 하면 한 달에 스타터 팩 하나를 더 번다"는 체감은 설명하기 쉽고 과하지 않다.
3. **A 는 문제를 못 고친다.** §1 의 실태(잔액 0 인 유저가 13명)에서 하루 193 Z 는 10연 한 판에 16일이다.
   지금 실수입이 30~90 Z/일이라 개선이긴 하지만, 수도꼭지를 여는 게 목적인데 물이 안 나온다.
4. **C 는 되돌리기가 어렵다.** 보상은 **올리기는 쉽고 내리기는 매우 어렵다**(내리면 너프로 읽힌다).
   금액이 전부 config 노브라 무배포로 올릴 수 있으므로, **B 로 열고 데이터를 보고 올리는** 쪽이 안전하다.

### 5.3 다른 다이아 축과의 서열 — 이 순서가 깨지면 의도가 무너진다

| 축 | 하루/회당 | 미션(확정안) 대비 |
|---|---|---|
| **미션 하루 상한** | **600 Z** | — |
| 리그 일일 트랙 이론 최대 | 1,080 Z | 미션이 **0.56배** ✔ |
| 리그 일일 트랙 **실측** | 30~90 Z | 미션이 7~20배 ⚠️ |
| 리그 시즌 완주 | 3,000 Z | 미션 **5일치** ✔ |
| 리그 우승(1위, 완주 포함) | 9,000 Z | 미션 15일치 ✔ |

⚠️ **미션이 리그 일일 트랙의 실측치를 7~20배 앞선다.** 이건 부작용이 아니라 **의도한 결과**다 —
리그 트랙은 하루 18칸이라 실플레이(1~4.5판)로는 닿지 않는 설계고, 미션은 1~3판에 맞춰 설계했다.
다만 **"리그를 열심히 하는 것보다 원정 미션 두 개가 낫다"** 가 되면 리그의 위상이 흔들리므로,
**계약으로 서열을 박제**한다:

| 계약 | 지금 값 |
|---|---|
| `미션 하루 상한 < 리그 일일 트랙 이론 상한` | 600 < 1,080 |
| `미션 하루 상한 < 리그 시즌 완주 보상` | 600 < 3,000 |

이 관계가 깨지는 노브 조정은 의도한 것이어야 하고, 그때 테스트가 먼저 깨진다(#368 이 쓴 방식 그대로).

---

## 6. 규칙

### 6.1 리셋 — KST 자정, 그런데 **리셋 잡은 만들지 않는다**

기존 일일 경계와 같은 기준을 쓴다: 원정 일일 한도(10판)·선수 컨디션·리그 일일 트랙 전부 **KST 자정**이고,
SoT 는 `ConditionService.dateOf(instant)` 다. 체감이 어긋나면 "어제 것"의 의미가 화면마다 달라진다.

⚠️ **리포에 "00:00 KST 에 도는 잡"이 하나도 없다.** 일일 의미는 항상 현재 KST 날짜로 lazy 계산한다.
미션도 **day-key 행**(`user_id, day`)으로 만들고, 자정이 지나면 **다음 조회 때 새 날짜의 행이 생긴다**.
리셋 스위퍼를 만들면 이 리포에서 유일하게 시계에 의존하는 부품이 되고, 서버가 잠깐 죽은 새벽에 조용히 건너뛴다.

**날짜 앵커는 경기 종료 시각**(생성 시각 아님). 23:58 에 시작해 00:03 에 끝난 경기가 어제 미션을 채우면
유저는 오늘 화면에서 그 판이 사라진 걸 본다. 이 버그류에 이 리포가 이미 두 번 당했다.

### 6.2 리롤 — 미션당 1회, 전체 풀에서 재추첨

**⚠️ 이 규칙은 §5.1(균등 랜덤 2개)과 한 쌍이다.** 쉬운 미션이 하나도 없는 날이 40% 인데, 리롤이
**그날을 구제하는 유일한 장치**다. 리롤 범위를 좁히려면 배분 규칙을 같이 다시 봐야 한다.

- 리롤하면 **진행도는 0 으로 초기화**된다(하스스톤과 동일). 이미 보유 중인 다른 미션과 **중복 추첨 금지**.
- 이미 **달성한 미션은 리롤할 수 없다**(보상만 챙기고 새 미션을 받는 무한 루프가 된다).
- 리롤 횟수는 **미션 슬롯에 매인다** — 자정에 새 미션이 오면 리롤도 다시 1회씩.

**"쉬운 걸로 갈아타는 어뷰징"은 성립하지 않는다.** 보상이 티어에 비례하므로 어려운 미션을 굴려 쉬운 걸 뽑으면
**받는 다이아가 같이 줄어든다**. 즉 리롤은 이득이 아니라 **"달성 가능성 ↔ 보상"의 선택**이다.
이게 티어 비례 보상표를 쓰는 두 번째 이유다(첫째는 §2 의 업계 관행).

> 대안 = **같은 티어 안에서만 재추첨**. 하루 기대 보상이 완전히 고정돼 예측 가능해지지만,
> 리롤이 "문구만 바뀌는 버튼"이 돼 재미가 없다. 권장하지 않는다.

### 6.3 이월 — 미션은 이월 없음, **보상은 안 사라진다**

- **미완료 미션**: 이월 없이 자정에 교체(요구 문구 *"하루마다 초기화"* 그대로). 진행도도 사라진다.
- **달성했는데 안 받은 보상**: **기한 없이 남는다.** 미션 행이 그날 날짜로 박제돼 있고 `claimed_at` 이 비어 있으면
  다음 날에도 받을 수 있다.
  - 이유: 마지막 경기를 치고 앱을 껐다는 이유로 보상을 잃으면 그건 버그로 읽힌다. 우편함도 **만료 기본 무기한**이다.
  - 이 규칙 덕에 **"자정에 미수령분을 지급하는 잡"이 필요 없다**(§6.1 과 같은 이유로 잡을 만들지 않는다).

### 6.4 신규 유저 첫 지급

**첫 조회 시점**에 그날 미션 2개를 만든다(가입 시점이 아니라). 가입 시점에 만들면 가입만 하고 안 들어온 유저의
미션이 매일 쌓이고, 그건 §6.1 의 lazy 원칙과도 어긋난다. **원정을 한 번도 안 해 본 유저에게도 미션은 보인다** —
그게 원정으로 끌고 가는 장치다.

### 6.5 포기(ABANDONED)·몰수는 세지 않는다

자발 포기는 정산 경로(`finishMatch`)를 아예 지나지 않고, 기존 선례가 포기를 **연승·시즌·재화에서 전부 제외**한다.
미션도 같게 간다. 안 그러면 "출전 3회" 미션을 **포기 3번으로 클리어**할 수 있다.

### 6.6 수령 — 자동 판정 + 수동 수령

- **판정은 서버가 자동**(경기 정산 시점). 클라가 "달성했다"고 신고하는 경로는 없다.
- **수령은 유저 액션**(`받기`). 다이아가 지갑에 꽂히는 순간을 유저가 만든다.
- 수령 UI 는 **#405(E2)의 확장형 보상 탭**을 쓴다 — 원정 경기 직후면 결과 보상 탭에 "미션 달성" 섹션으로,
  그 외 시점이면 원정 페이지의 미션 목록에서. (인터페이스는 #405 에 코멘트로 조율 중.)

---

## 7. hero 확정 (2026-08-02, 세션 hmb:mission)

| # | 질문 | **확정** | 세션 권장이었나 |
|---|---|---|---|
| **Q1** | 보상 금액 | **100 / 200 / 300 Z** | ✅ 권장과 동일 |
| **Q2** | 미션 배분 | **완전 랜덤 2개**(14종 균등, 중복만 금지) | ❌ 세션 권장은 "쉬움 1 + 보통·어려움 1" — **기각됨**(변동폭이 없으면 뽑는 재미가 없다) |
| **Q3** | 미션 범위 | **원정 전용** | ✅ 권장과 동일 |
| **Q4** | 리롤 재추첨 | **전체 풀에서** | ✅ 권장과 동일 |
| **Q5** | 화면 위치 | **원정 페이지 안 섹션** + 홈에 "받을 보상 N건" 한 줄 | ✅ 권장과 동일 |
| **Q6** | 미션 문구 14개 | §4 표 그대로 진행(수정 요청 시 **그 문장 그대로** 반영) | — |

**Q2 가 기각된 결과 무엇이 달라졌나** — 하루 보상이 300~400 고정에서 **200~600 변동**이 됐고,
"1판짜리 미션이 하나도 없는 날"이 **40%** 생겼다. 그 위험은 **리롤(§6.2)이 흡수한다.**
⇒ **배분 규칙과 리롤 정책은 이제 한 쌍이다.** 둘 중 하나만 바꾸는 변경은 하지 않는다.

---

## 8. 기술 스케치 (참고 — 게이트 대상 아님)

컨펌 후 이 방향으로 구현한다. **전부 기존 선례 재사용이고 새로 발명하는 건 미션 상태 하나뿐이다.**

### 저장 — 왜 파생이 아니라 테이블인가

미션 진행도는 매일 `matches` 에서 다시 계산할 수도 있다. 그럼에도 **행을 박제한다**. 근거는 `league_daily_rewards`·
`starter_grants` 와 같다: **금액·티어·풀 구성이 전부 config 노브**라, 읽을 때마다 재계산하면 운영이 노브를 돌리는
순간 **오늘 이미 받은 보상의 이력이 소급 변조된다**("아까 200 받았는데 화면엔 100"). **`amount` 와 `tier` 를 행에 박제**한다.

```sql
-- 실제 구현(V40__daily_missions.sql). 초안에서 **네 군데가 넓어졌다** — 근거는 바로 아래.
CREATE TABLE daily_missions (
  id          TEXT PRIMARY KEY,          -- ULID
  user_id     TEXT NOT NULL,
  day         TEXT NOT NULL,             -- 'yyyy-MM-dd' (KST)
  slot_no     INTEGER NOT NULL,          -- 1 | 2
  mission_id  TEXT NOT NULL,             -- 'away_streak_2' 등 (카탈로그 키)
  title       TEXT NOT NULL,             -- ← 추가: 화면 문구 (박제)
  tier        TEXT NOT NULL,             -- 'EASY'|'NORMAL'|'HARD'  (박제)
  rule        TEXT NOT NULL,             -- ← 추가: 판정 규칙       (박제)
  currency    TEXT NOT NULL,             -- 'GEM'                   (박제)
  amount      INTEGER NOT NULL,          -- 그 미션의 보상액         (박제)
  target      INTEGER NOT NULL,          -- 박제(카탈로그가 바뀌어도 오늘 목표는 안 바뀐다)
  progress    INTEGER NOT NULL DEFAULT 0,
  completed_at TEXT, claimed_at TEXT, rerolled_at TEXT,
  created_at  TEXT NOT NULL,
  FOREIGN KEY (user_id) REFERENCES users(id)
);
-- ← 부분 유니크: **살아 있는** 미션만 슬롯을 차지한다(은퇴 행은 이력으로 남는다)
CREATE UNIQUE INDEX uq_daily_mission_slot
    ON daily_missions(user_id, day, slot_no) WHERE rerolled_at IS NULL;
CREATE INDEX idx_daily_mission_user_day  ON daily_missions(user_id, day);

-- ← 추가 테이블: 경기 한 판이 미션 하나를 얼마나 밀었나
CREATE TABLE daily_mission_progress (
  match_id        TEXT NOT NULL,
  mission_row_id  TEXT NOT NULL,
  progress_before INTEGER NOT NULL,
  progress_after  INTEGER NOT NULL,
  created_at      TEXT NOT NULL,
  PRIMARY KEY (match_id, mission_row_id),
  FOREIGN KEY (mission_row_id) REFERENCES daily_missions(id)
);
```

**초안에서 넓어진 네 가지와 근거** (구현 중 확정):

| 추가 | 왜 |
|---|---|
| `title` · `rule` 박제 | §6.3 이 "달성했는데 안 받은 보상은 **기한 없이** 남는다"이고 §9 롤백이 "카탈로그를 비운다"이므로, **카탈로그에서 사라진 미션의 미수령 행**이 반드시 생긴다. 그때 문구를 카탈로그에서 조회하면 화면에 빈 제목이 뜨고, 판정 규칙을 조회하면 그날 남은 경기에서 진행도가 안 오른다. **행 하나만 읽어도 표시·판정·지급이 완결**되어야 한다 |
| 슬롯 유니크를 **부분 인덱스**로 | 리롤을 제자리 UPDATE 로 하면 진행도 원장이 가리키는 행의 미션이 사후에 바뀌어, **지난 경기 결과 화면이 "그 경기가 밀지도 않은 미션"을 그린다**. 그래서 리롤 = 은퇴 + 새 행이고, 은퇴 행은 유니크 제약 밖으로 빠져야 한다. 리롤 소진은 별도 카운터가 아니라 **그 슬롯의 은퇴 행 수**로 센다(두 값이 갈라질 수 없다) |
| `daily_mission_progress` 신설 | ①**멱등** — 재정산이 진행도를 두 번 올리면 "출전 3회"가 한 판으로 끝난다(`league_daily_rewards.match_id` PK 와 같은 층, 여기선 경기×미션 축이라 복합 PK) ②**결과 화면의 `missions` 배열** — "이 경기가 얼마나 밀었나"는 누적 진행도에서 **사후에 분해되지 않는다**(1→2 인지 0→2 인지 알 길이 없다). GET 은 몇 번을 불러도 같은 답을 해야 하므로 델타를 행으로 남긴다 |
| ~~`completedNow` 컬럼~~ (두지 않음) | `progress_before < target ≤ progress_after` 로 파생되고 `target` 이 박제라 두 값이 갈라질 수 없다 |

- 마이그레이션 번호 = **V40** (V39 에서 개번). `FlywayMigrationTest` 기대 목록에도 등록.
  ⚠️ 이력: main 이 2026-08-02 에 V38 을 #405, V39 를 #408 에 배정했는데 **#405 가 머지되며 V38·V39 를 둘 다** 가져가서 V40 으로 옮겼다. 아직 배포되지 않은 마이그레이션이라 리넘버가 안전하다(#248 이 V23→V25→V26 으로 두 번 옮긴 것과 같다).
  ⚠️ 리베이스 전까지 `FlywayVersionContinuityTest` 에 뒀던 **예약 목록**(`RESERVED_BY_OTHER_BRANCH = {38: "#405"}`)은 **제거했다** — 결번이 사라졌으니 연속성 검사가 제 힘으로 선다. 예외 목록은 그 자체가 사각지대라(열거된 번호에서 검사가 눈을 감는다) 필요가 끝나면 바로 지운다.
- 미션 추첨은 **시드 RNG**(`sha256(userId:day:slotN)` 앞 15 hex → 후보 수로 나눈 나머지, `UserOnboardingService.pickStarterTop` 과 같은 계열) — 결정론이라 "왜 이 미션이 나왔나"를 재현할 수 있다.
- **카탈로그는 `application.yml hmb.mission.daily.*`, 금액만 economy.** 값의 성격이 다르다: 금액은 economy override + reload 로 **무배포** 조정하는 경제 곡선이고, 카탈로그(id·티어·판정규칙·목표·문구)는 게임 규칙의 **구조**라 바뀌면 판정 코드(`MissionRule`)와 같이 움직여야 한다. #245 가 `hmb.away.reward.mode` 는 application.yml 에 두고 **금액은 economy 를 참조**한 것과 같은 갈라짐이다.
  ⚠️ economy 는 `data/players/economy.v3.json` 에 **손으로 얹었다**(`mission.reward`). 이건 #251·#368 이 같은 파일에 한 것과 같은 방식이다 — `data/players/generate.ts` 의 `economyV3` 는 v2 를 그대로 복사할 뿐이라 **재생성하면 #251/#368 블록이 통째로 사라진다**(그래서 `data.test.ts` 의 바이트 동일성 목록에도 economy.v3 이 없다). 생성기와 발행물이 갈라진 상태이고 **#408 이 만든 문제가 아니다** — data 도메인에 이슈 레이즈가 필요하다.

### 지급 — 새 원장을 만들지 않는다

`walletService.applyGems(userId, amount, "daily_mission", dailyMissionRowId)`.
멱등은 **3층**: ① 수령 CAS(`UPDATE … WHERE claimed_at IS NULL`) ② 슬롯 유니크 ③ 원장 `uq_gem_ledger_reason_ref`.
`V33__mailbox.sql` 의 규칙 그대로 — *"왜 다이아가 늘었나"의 답이 두 곳이 되면 안 된다.*

### 판정 훅

`MatchOrchestrator.finishMatch` 의 **FINISHED CAS 통과 이후**, 원정 정산(`awayService.settle`) 바로 옆.
`mode == "away"` 일 때만. 그 자리에서 `result`·`userGoals`·`oppGoals` 가 이미 계산돼 있다.
"선제골" 판정만 매치로그 파싱이 필요하고, 그건 `GrowthService.eventCountsByPlayer` 가 같은 트랜잭션에서 하는 일과 같다.

**판정 규칙은 일곱 갈래**(`MissionRule`)로 14종을 전부 표현한다 — 미션마다 분기를 쓰면 카탈로그를 늘릴 때마다 판정 코드를 고쳐야 하고, 그때 **이미 발급된 행의 판정이 바뀐다**.

| rule | 전이 | 쓰는 미션 |
|---|---|---|
| `PLAY` | +1 (승패 무관) | `away_play_{1,2,3}` |
| `WIN` | WIN 이면 +1 | `away_win_{1,2,3}` |
| `WIN_STREAK` | 승 +1 · **패 0 으로 끊김** · **무승부 유지** | `away_streak_{2,3}` |
| `BEST_MATCH_GOALS` | `max(progress, userGoals)` — **합계가 아니다** | `away_goals_{2,3,4}` |
| `CLEAN_WIN` | `WIN && oppGoals==0` 이면 +1 | `away_clean_win` |
| `BEST_WIN_MARGIN` | `max(progress, WIN ? userGoals-oppGoals : 0)` | `away_margin_3` |
| `FIRST_GOAL` | 매치로그 **첫 goal 이벤트**가 우리 팀이면 +1 | `away_first_goal` |

- **연승의 하루 경계**는 별도 집계가 필요 없다 — 미션 행이 `day` 로 잘려 있어 `progress` 자체가 "그날 안의 연승"이다(`away_streaks` 는 통산이라 그대로 쓸 수 없다). **무승부 유지**는 `AwayService` 통산 연승(hero E4)과 같은 규칙으로 맞췄다 — 두 화면이 서로 다른 "연승"을 말하면 유저는 어느 쪽도 믿지 않는다.
- **달성 후에는 진행도가 언다**(`UPDATE … WHERE completed_at IS NULL`). 안 그러면 나중 패배가 연승 미션을 0 으로 되돌려 이미 딴 보상이 사라진다.
- **§6.4 를 "첫 조회 → 첫 조회 *또는* 첫 정산"으로 넓혔다.** 그러지 않으면 앱을 안 켜고 원정만 친 유저의 진행도가 통째로 사라진다(그날 미션 행이 없으니 밀 대상이 없다). 추첨이 시드 결정론이라 **어느 쪽이 먼저 만들어도 같은 두 미션**이라 §6.1 의 lazy 원칙은 그대로다.

### 조회 계약 — 클라는 아무것도 계산하지 않는다

`GET /api/missions/daily` 가 **문구·진행도·목표·보상액·상태를 완성해서** 내려준다.
클라가 티어→금액을 다시 계산하면 노브를 돌린 순간 화면이 거짓말한다(#368 이 같은 이유로 18칸을 통째로 내려준다).

```jsonc
// GET /api/missions/daily
{
  "day": "2026-08-02",                       // KST
  "resetAtKst": "2026-08-03T00:00:00+09:00", // 화면의 "자정에 초기화" 문구용 — 클라가 계산하지 않는다
  "missions": [
    {
      "id": "01J...",                        // daily_missions 행 id (claim·reroll 의 키)
      "missionId": "away_streak_2",          // 카탈로그 키(분석·디버깅용, 화면 표시 아님)
      "title": "원정 2연승",                  // 서버가 완성한 문구
      "tier": "NORMAL",                      // 'EASY'|'NORMAL'|'HARD' — 배지 색/라벨용
      "currency": "GEM", "amount": 200,      // 재화와 금액은 항상 같이 온다(#232)
      "progress": 1, "target": 2,
      "state": "IN_PROGRESS",                // 'IN_PROGRESS'|'COMPLETED'|'CLAIMED'
      "rerollable": true                     // 서버 판단(1회 소진·달성 여부를 클라가 추론하지 않는다)
    }
  ],
  "pendingClaims": [                         // ← 지난 날짜의 달성·미수령분(오늘 것은 missions 에 있다)
    { "id": "01J…", "day": "2026-08-01", "missionId": "away_win_2", "title": "원정에서 2승",
      "tier": "NORMAL", "currency": "GEM", "amount": 200 }
  ],
  "claimableCount": 1,                       // 홈 "받을 보상 N건" 한 줄 = missions 의 COMPLETED + pendingClaims
  "claimableAmount": 200
}
```

- **`claimableCount` 는 오늘 것만이 아니다.** §6.3 대로 달성분은 기한 없이 남으므로 어제·그제 미수령분을 합산한다.
  안 그러면 홈 한 줄이 "받을 게 없다"고 말하는데 미션 화면엔 받을 게 있는 상태가 된다.
- ⚠️ **그래서 `pendingClaims` 가 반드시 같이 온다.** 합계만 주고 목록을 안 주면 **정확히 반대 방향의 같은 버그**가 된다 —
  홈은 "받을 보상 1건"이라는데 미션 화면엔 받을 카드가 없다. 수령은 오늘 것과 **같은 엔드포인트**를 쓴다
  (`claim` 은 날짜를 보지 않는다. 날짜를 보는 건 리롤뿐 — 지난 미션은 교체 대상이 아니다).
- **구 서버 폴백**: 이 블록이 없으면 web 은 섹션을 **통째로 안 그린다**(#286 W5 규율 — 스켈레톤·에러를 띄우면
  "아직 없는 기능"이 "고장 난 화면"이 된다).

```jsonc
// POST /api/missions/{id}/claim   → 200
{ "claimed": { "currency": "GEM", "amount": 200 }, "wallet": { "points": 3000, "gems": 12200 } }
// 이미 받았으면 409 MISSION_ALREADY_CLAIMED · 미달성이면 409 MISSION_NOT_COMPLETED

// POST /api/missions/{id}/reroll  → 200
{ "mission": { …새 미션 객체(위 형태와 동일)… } }
// 리롤 소진 409 MISSION_REROLL_USED · 달성한 미션 409 MISSION_ALREADY_COMPLETED
```

**구현이 계약보다 코드 두 개가 많다** — §8 이 열거하지 않은 상태이고, 각각 유저가 취할 행동이 다르다(#286 이 복수에서 세 개를 더한 것과 같은 규율):

| 코드 | 상태 |
|---|---|
| 404 `NOT_FOUND` | 없는 미션 **과 남의 미션**. 갈라 두면 id 실재가 새어 나간다(#297·#323) |
| 410 `MISSION_EXPIRED` | **지난 날짜**의 미션을 리롤하려 함. 그날은 끝났고 남은 건 수령뿐이다 — 여기에 `MISSION_REROLL_USED` 를 쓰면 거짓말이다 |
| 409 `MISSION_REROLL_UNAVAILABLE` | 풀이 보유분보다 작아 뽑을 후보가 없음(14종 ≫ 2 라 현행 config 에선 도달 불가, 카탈로그를 줄이면 열린다) |

### 결과 화면 — `GET /api/matches/{id}/result` 에 additive 필드

#368 이 `dailyReward` 를 얹은 자리와 같은 규율. **구 클라는 필드를 무시하면 그만이라 배포 순서 결합이 없다.**

```jsonc
{ …, "missions": [ { "id": "01J…", "title": "원정 2연승", "tier": "NORMAL",
                     "currency": "GEM", "amount": 200,
                     "progress": 2, "target": 2, "completedNow": true,
                     "state": "COMPLETED" } ] }
```

`completedNow` = **이 경기로 달성됐다**(이전에 이미 달성된 것과 구분). 진행만 오른 미션도 배열에 포함한다 —
"이 경기로 미션이 얼마나 갔나"를 보여주는 게 결과 화면의 일이다. 이 배열이 #405 보상 탭의 **미션 섹션** 원료다.

⚠️ **`state` 가 없으면 결과 화면에서 수령을 열 수 없다.** §6.6 은 원정 직후 결과 화면에서 받는 흐름을 말하는데,
`state` 가 빠지면 web 이 "지금 받을 수 있나"를 `progress >= target` 으로 **재계산**해야 하고 그건 이 문서가
금지한 바로 그 짓이다(수령한 뒤에도 계속 "받기"가 보인다). ⇒ `state` 는 이 배열의 필수 필드다.

### 계약 (E2E-TDD — 구현 전에 박는다)

| 계약 | 무엇을 죽이나 |
|---|---|
| 자정 경계가 **KST** 이고 앵커가 **종료 시각** | UTC 경계 변이(이 리포가 두 번 당한 버그) |
| 금액이 **config 에서** 오고 **행에 박제**된다 | 상수 하드코딩 · 노브를 돌리면 지난 지급이 바뀌는 변이 (픽스처 금액을 발행값과 **다르게** 둬서 관측) |
| 수령 2회차가 지갑을 안 움직인다 | 멱등 3층 중 어느 하나라도 뚫림 |
| 리롤은 미션당 1회 · **달성한 미션은 리롤 불가** · 진행도 초기화 | 무한 리롤 · 보상 이중취득 |
| **포기한 경기는 진행도를 안 올린다** | "출전 3회"를 포기 3번으로 클리어 |
| 하루 2개가 **서로 다른 미션** · 추첨이 14종 전체에서 균등 | 중복 배분 · 풀 일부가 영영 안 나오는 변이 |
| 하루 상한(600 Z)이 §5.3 서열을 안 깬다 | 경제 서열 역전(리그 축을 미션이 추월) |
| **실제 원정 경기를 끝내면 진행도가 오른다**(전 경로) | **훅 제거** — 서비스 단위 계약만 있으면 아무도 안 부르는 상태가 전부 통과한다 |

전 계약은 **변이체 킬 검증**을 거친다(구현을 되돌리면 실제로 죽는지 확인). 이 리포에 "통과만 하는 장식 계약" 사고가 여러 번 있었다.

---

## 9. 되돌리기

- **끄기**: **`hmb.mission.daily.count: 0`**(env `HMB_MISSION_DAILY_COUNT=0`) = 새 날짜의 미션이 생성되지 않는다.
  화면은 섹션을 통째로 안 그리고, **이미 달성한 미수령 보상은 그대로 받을 수 있다**(§6.3 — 끄기가 지갑을 뺏지 않는다).
  금액만 0 으로 내리는 방법은 **쓰지 않는다** — 미션은 뜨는데 보상이 0 이면 고장으로 읽힌다.
  ⚠️ 초안은 "카탈로그를 비우면"이었는데, **YAML 리스트는 env·property 하나로 비울 수 없다** = 그 방식은 재배포를 요구하므로 롤백 수단이 아니다. 카탈로그가 비어도 같은 결과가 되도록 방어는 남겼다.
  계약 = `MissionRollbackOffTest`(끄기 + 끈 뒤 수령 가능).
- **금액 조정**: economy 노브(무배포). 박제 규율 덕에 **이미 지급된 것은 소급 변조되지 않는다**.
- **코드 롤백**: `finishMatch` 의 한 줄 + 라우트 등록 + web 섹션 한 줄.

---

## 10. 변이체 킬 검증 (server-java 웨이브, 21/21 사망)

계약이 "통과만 하는 장식"이 아닌지 구현을 되돌려 실측했다. **초판에서 3건이 살아남았고 셋 다 계약을 고쳤다** — 그 세 건이 이 리포가 반복해 온 함정의 실례라 기록해 둔다.

| 살아남았던 변이 | 왜 안 죽었나 | 어떻게 고쳤나 |
|---|---|---|
| 생성 지점의 금액을 상수 100/200/300 으로 | 금액 계약이 **테스트 헬퍼가 심은 행**을 읽고 있었다 = 생산 경로(추첨→insert)를 한 번도 안 탔다 | 미션을 심지 않고 **`daily()` 로 생성된 행**의 금액을 40명분 관측(픽스처 11/22/33) |
| 달성 후에도 진행도를 다시 계산 | DB 의 `WHERE completed_at IS NULL` 가드가 막아 **DB 단정만으로는** 관측이 안 됐다(응답에는 틀린 값이 실린다) | `settle` **반환값**(결과 화면 payload)의 progress 도 단정 |
| 리롤을 같은 티어 안에서만 | "리롤 결과 티어가 셋 다 나온다"가 **어차피 참** — 원래 미션 티어가 이미 셋에 걸쳐 있다 | 관계식으로 교체: **티어를 건너뛴 리롤이 실제로 일어나는가** |

### W4 후속(계약 갭 2건 마감) — 11/12 사망 · 등가 1

W3(web)이 **계약 자체의 구멍 2개**를 드러냈다: ①`claimableCount` 는 전 기간인데 목록은 오늘 것뿐이라
어제 미수령분이 **화면에서 도달 불가능**했다 ②결과 화면 미션에 `state` 가 없어 web 이 수령 버튼을 못 달았다.

| 변이 | 죽은 계약 |
|---|---|
| `pendingClaims` 가 오늘 것까지 실음(중복) | `todaysCompletedMissionLivesInMissionsOnly…` · `claimableTotalsAlwaysMatch…` |
| `pendingClaims` 를 아예 안 실음(**원래의 갭**) | 위 + `pendingClaimsCarriesPastDays…` · `pendingClaimsAreOrderedOldestFirst` · `retiredRowsNeverAppear…` · `completedRewardsSurvive…` |
| 합계가 은퇴 행까지 셈 | `retiredRowsNeverAppearInPendingClaims` |
| 합계가 오늘 것만 셈(**원래 갭의 반대 방향**) | `claimableTotalsAlwaysMatch…` · `retiredRowsNeverAppear…` |
| 합계가 이미 받은 것도 셈 | `claimableTotalsAlwaysMatch…` · `completedRewardsSurvive…` |
| `pendingClaims` 가 은퇴 행도 실음 | `retiredRowsNeverAppearInPendingClaims` |
| `pendingClaims` 정렬이 최신순 | `pendingClaimsAreOrderedOldestFirst` |
| 결과 화면 `state` 를 상수로 | `resultMissionsCarryStateSoTheScreenNeverRecomputesIt` |
| 수령한 미션이 결과 화면에서 여전히 COMPLETED | 〃 |
| `claim` 이 날짜를 봄(지난 보상 수령 불가) | `pendingClaimsCarriesPastDaysUnclaimedRewardsSoTheyStayReachable` |

⚠️ **등가 변이 1건을 숨기지 않고 적어 둔다**: 합계를 "화면에서 파생"이 아니라 **의미가 같은 독립
쿼리**(전 기간 · 미수령 · 비은퇴)로 되돌리는 변이는 **아무 테스트도 죽이지 않는다** — 두 계산이
정의상 같은 집합을 세기 때문이다. 파생형을 쓰는 이유는 지금 값이 달라서가 아니라 **구조적**이다:
원본이 하나면 나중에 한쪽만 고치는 드리프트가 열리지 않는다. 그 드리프트를 실제로 만드는 변이
세 종류(은퇴 포함 · 오늘만 · 수령분 포함)는 전부 죽는다. **"변이체가 죽었다"와 "이 줄이 계약으로
지켜진다"는 다른 말**이고, 후자를 주장할 수 없을 땐 그렇게 적어야 한다(#286 규율).

### W5 후속(독립검증 minor-1/2/3 — 호출부 커버리지) — 3/3 사망

검증자가 **미션 계약 55건을 전부 통과하는 변이 2종**을 실증했다. 라이브 결함이 아니라 **계약이
호출부를 안 태우고 있던 것**이다 — 기존 계약들이 `finishedAt`·`userHome` 을 **서비스에 직접 넘겨서**
"호출자가 무엇을 넘기나"는 한 번도 관측되지 않았다.

| 변이 | 죽은 계약 |
|---|---|
| `clockService.now()` → `Instant.parse(match.createdAt())` (§6.1 이 이름 붙여 경계한 버그) | `aMatchThatCrossesMidnightFeedsTheDayItFinishedNotTheDayItStarted` |
| `userHome` → `!userHome` (선제골 기준 사이드 반전) | `firstGoalIsJudgedFromTheUsersSideAllTheWayThroughTheFinishHook` |
| 서비스가 `dateOf(finishedAt)` 대신 `today()` (검증자 판정 = **등가**) | `settleAnchorsToTheGivenFinishTimeNotToWhateverTimeItIsNow` |

- ⚠️ 첫 변이는 **하루를 빼는** 쪽은 죽었는데 `createdAt` 쪽은 살았다 = **"그럴듯하게 틀린 값"만
  통과**하는 상태였고, 그게 가장 나쁜 종류의 구멍이다.
- 세 번째는 검증자가 등가로 분류했지만 **계약을 하나 더해 죽였다**: 유일 호출자가 `now()` 를 넘겨
  값이 같을 뿐, 그 상태에선 **파라미터가 장식**이라 재정산 도구·백필 같은 두 번째 호출자가 생기는
  순간 조용히 오늘 미션을 민다. "호출자가 무엇을 넘기나"(`MissionFinishHookWiringTest`)와 "서비스가
  그걸 쓰나"(`MissionDailyTest`)는 **다른 계약**이고, 둘이 붙어야 §6.1 의 날짜 앵커가 완전히 박힌다.
- 새 계약은 **서비스에 아무것도 직접 넘기지 않는다** — 시각도 사이드도 실제 경기 흐름(HTTP 킥오프 →
  엔진 → 시계 스윕 → FINISHED CAS)이 정하게 두고 결과만 본다. 생성일 ≠ 종료일 표본은 주입 `Clock` 을
  KST 자정 너머로 옮겨 만든다(간격은 2분 — `stale-after-min` 스위퍼에 표본이 먹히지 않게).

---

최종 W1~W2 21/21 · W4 11/12(등가 1) · W5 3/3 사망. 전체 표는 웨이브 보고 참조(훅 제거 · 훅을 몰수 경로에 이식 · UTC 자정 · 정산 멱등 제거 · 수령 CAS 약화 · 미달성 수령 · 달성 미션 리롤 · 무한 리롤 · 무승부가 연승을 끊음 · 한 경기 N골을 합계로 · 골 차가 패배도 셈 · 선제골이 사이드 무시 · 추첨 편향 · 결과 화면 뷰어 미스코프 · 롤백 스위치 무시 · economy 폴백 0원 · lazy 생성 제거).
