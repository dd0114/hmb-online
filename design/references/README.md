# design/references — 레퍼런스 이미지 컨벤션
여기에 넣은 이미지는 designer 에이전트가 **매 작업 시작 시 자동으로 로드해 톤을 흡수**한다.
- 파일명에 용도 태그: `ui-*.png`(화면 톤), `char-*.png`(캐릭터/스프라이트), `fx-*.png`(연출).
- 출처가 상용 게임이면 "참고용"임을 인지 — 트레이싱/복제 금지, 감성 참고만.
- 컨셉 확정 후에는 `design/art-direction.md`(SoT)가 우선하고, 레퍼런스는 보조가 된다.
