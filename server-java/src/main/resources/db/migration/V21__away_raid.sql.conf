# V8/V19 와 같은 이유 — SQLite 12단계 테이블 재작성(matches.mode CHECK 확장)은
# PRAGMA foreign_keys=OFF 가 필요하고 그 PRAGMA 는 트랜잭션 안에서 무효다. Flyway 기본
# (트랜잭션 감쌈)이면 DROP TABLE matches 가 올린 FK 위반이 COMMIT 에서 터진다 — #245.
executeInTransaction=false
