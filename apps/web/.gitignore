# Logs
logs
!/src/logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# integration smoke screenshots/logs (not build artifacts, just QA evidence)
.smoke/
# #148 매치 컨트롤 실캡처(검증 증적)
.matchui/
# #207 어드민 유닛 카탈로그 실캡처(검증 증적)
.admin-units/

# QA 뷰어 임베드 아티팩트 — `npm run build:viewer` 로 재현(엔진 결정론 생성물). 엔진 버전 바뀌면 재빌드.
public/viewer-embed.html

# 캐릭터 에셋 스테이징 — `npm run build:chars` 로 재현(design/characters/dist 복사본).
public/chars/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# #245 hero 컨펌용 실화면 캡처 — 이 모듈의 QA 증적 관례(dot-prefix + ignore)를 따른다.
.p245-capture/
# #421 스킵 모드 실화면 캡처(`e2e/p421-skip.capture.ts` 산출) — 같은 관례.
.p421/
# #424 흐름 브릿지 4지점 실화면 캡처(`e2e/p424-bridges.capture.ts` 산출).
.p424/
