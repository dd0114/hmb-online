import { PlaceholderPage } from "../common/PlaceholderPage";

/** LLD-web §4-W1. 110명 그리드(등급/포지션 필터)는 W1에서 구현. */
export function CodexPage() {
  return <PlaceholderPage title="도감" note="W1에서 구현됩니다." />;
}
