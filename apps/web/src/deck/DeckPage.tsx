import { PlaceholderPage } from "../common/PlaceholderPage";

/** LLD-web §4-W1. Slot grid + bench + prompt editor land in W1. */
export function DeckPage() {
  return <PlaceholderPage title="덱 구성" note="W1에서 구현됩니다." />;
}
