node_modules/
.env
.DS_Store
dist/
*.log
!evidence/**/*.log
packages/engine/dev-viewer/match-log.json
packages/engine/dev-viewer/viewer-standalone.html
tools/shots/
# Playwright 출력(루트)
test-results/
playwright-report/
# 관전 화면 실캡처 증빙(#169 S1) — 재생성 가능한 로컬 산출물
apps/web/.stage/
apps/web/.p405/
apps/web/.stage-capture/
apps/web/.p233/
tools/qa_*.mjs
# QA 임시 캡처 디렉토리. ⚠️ 구 규칙(`tools/qa_*.mjs`)은 **스크립트만** 걸러서, 독립 QA 가 남긴
# 스크린샷 11개가 다른 세션 커밋에 `git add -A` 로 딸려 들어갔다(#365). 디렉토리째 막는다.
tools/qa_shots_tmp/
tools/reqa*.mjs
tools/*_log.json
research/engine-realism-gap.data.md
# design 세션 격리 스택 DB(로컬 전용)
server-java/.data-design/

# 배포 매니페스트(#164) — 에페메럴 터널 URL 포함, 생성물
infra/deploy-manifest.json

# QA 콘솔(#191) 세션 로컬 산출물 — before/after 비교용 로그 사본 등(재생성 가능)
.qa/
# 레지스트리 폴백 경로(정상 위치는 리포 밖 ~/hmb-qa-console — §3.1). 실수로 리포에 생겨도 커밋되지 않게.
.qa-console/

# 공지 팝업 실화면 캡처(#292) — Playwright 산출물, 재생성 가능
apps/web/e2e-out/

# Cloudflare Pages Function 배치본(#299). SoT 는 infra/pages/functions/ 이고 여기는 build.sh 가
# 만드는 생성물이다 — wrangler 가 `process.cwd()/functions` 만 보기 때문에 리포 루트여야 한다.
/functions/
# wrangler 작업 디렉토리(cwd 아래에 만든다)
.wrangler/

# #377 M2 관전 증거 로그 — 생성물(결정론적으로 재생성 가능, 22MB)
# 재생성: npx tsx evidence/377/gen-m2.ts
evidence/377/m2-*.json
evidence/377/m1-main-*.json
evidence/377/m3a-*.json
evidence/377/m3b-*.json
evidence/377/m3c-*.json

# #450 보상 검증용 라이브 DB 사본(683MB) — 증빙 로그만 커밋한다
evidence/450/live-snapshot/
