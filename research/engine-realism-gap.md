# 엔진 리얼리즘 갭 분석 (E3 · Track α)

> **목적**: 현 엔진(`engine@0.16.0` = `defaultEngineConfig` 리얼 config)의 게임 양상을 실제 축구
> (`research/football-stats.md`, 주로 PL/Opta) + 축구게임 엔진(FM/ESMS 관례)과 대조해 **잔여 갭 +
> 다음 리얼리즘 프론티어**를 데이터로 확정한다(헛튜닝 방지). SoT 에픽 = GH #99 · 상위 QA #25.
>
> **방법**: 리얼 config 로 20개 고정 시드(90분) 시뮬 → 팀-경기(=시드×2 = 40) 평균 스탯을 벤치와 대조.
> 하니스 = `packages/engine/src/realism/harness.ts`(computeMatchStats 재사용 + 점유율·패스길이·xG 재구성).
> 재생성: `HMB_GEN_GAP=1 npx vitest run packages/engine/src/realism/gap-report.test.ts` → `engine-realism-gap.data.md`.
> **최신 조사일: 2026-07-21 · `engine@0.16.0` · lastHash `70b7ea61`**(결정론 앵커). (구 조사 0.10.0 lastHash `dda2d08e`.)

---

## 1. 대조표 (리얼 config `engine@0.16.0`, 20 시드 · 팀-경기 40)

| 지표 (팀·경기) | 엔진 | ±SD | 벤치마크 | 판정 |
|---|---|---|---|---|
| **패스 성공률** | 79.47% | 1.84 | 78–85% | **OK** |
| **의도적 롱패스 시도(detail=long)** | 14.33% | 1.75 | 12–15% | **OK** |
| 롱볼 이동(≥30m, 재구성) | 20.06% | 1.13 | 12–15% (노이즈 참고) | HIGH ▲5.06 *(하드게이트 아님, §3)* |
| 점유율 | 50.0% | 2.81 | 30–65% (대칭→~50) | OK |
| **슛(시도)** | 14.0 | 5.59 | 12–14 | **OK** (상단) |
| 유효슛 | 6.08 | 3.13 | 4.5–5.5 | HIGH ▲0.58 *(~1.2σ)* |
| 유효슛 비율 | 42.56% | 14.98 | 45–50% | LOW ▼2.44 *(~1.0σ)* |
| **골** | 1.65 | 1.39 | 1.4–1.65 | **OK** (상단) |
| 슛→골 전환 | 12.19% | 10.28 | 10–12% | HIGH ▲0.19 *(노이즈 내)* |
| 슛당 xG | 0.11 | 0.02 | 0.10–0.12 | OK |
| **코너** | 5.38 | 2.84 | 4–6 | **OK** |
| **스로인** | 16.0 | 4.18 | 17–19 | LOW ▼1.00 *(~1.5σ)* |
| **파울** | 9.68 | 3.42 | 11–12 | **LOW ▼1.32 *(~2.4σ, 잔여 최상위)*** |
| 오프사이드 | 1.88 | 1.38 | 1–3 | OK |
| 옐로카드 | 2.05 | 1.14 | 1.8–2 | HIGH ▲0.05 *(상단 경계)* |
| 팀 width | 49.37m | 0.43 | 40–50m | OK |
| 팀 length | 40.3m | 0.81 | 25–40m | HIGH ▲0.30 *(상단 경계)* |
| 주행거리 | 9.9km | 0.27 | 10–12km | LOW ▼0.10 *(경계)* |

- **골(양팀/경기)**: **3.3** (벤치 ~2.7–3.3, 상단).
- **패스 길이 분포(팀·경기 평균, 소유권 이전 재구성)**: short <15m **33.5%** · medium 15–30m **46.5%** · long ≥30m **20.1%**.
- 유의성 주: 평균은 40 팀-경기 표본 → 표준오차 SE ≈ SD/√40. 파울 SE≈0.54(**갭 2.4σ**) · 유효슛 SE≈0.50(1.2σ) · 스로인 SE≈0.66(1.5σ) · 유효슛비율 SE≈2.37(1.0σ). **파울만 ~2σ 초과의 유의 갭**, 나머지 out-of-band 는 대부분 <1.5σ(노이즈 내) 또는 밴드 상/하단 경계.

---

## 2. 0.10.0 → 0.16.0 진척 & 회귀 판정

### 2.1 밴드화(회귀 가드) 항목 — **전부 유지, 회귀 없음** ✅
E1(0.11.0)·E2(0.12.0)·슛과다(0.15.0)·좌우대칭(0.16.0) 픽스로 밴드화된 항목이 0.16.0 에서 밴드를 유지하는지. `packages/engine/src/realism/{shot-frequency,pass-accuracy,lateral-balance}.test.ts` 14 케이스 **전부 green**.

| 밴드화 항목 | 0.10.0 (픽스 전) | 0.16.0 (현재) | 밴드 | 회귀 가드 |
|---|---|---|---|---|
| 패스 성공률 | 85.99% HIGH | **79.47% OK** | 78–85 | pass-accuracy B ✅ |
| 의도적 롱패스(detail=long) | 0% (모델 없음) | **14.33% OK** | 12–15 | pass-accuracy E2 ✅ |
| 슛(시도) | 19.5 HIGH | **14.0 OK** | 12–14.5 | shot-frequency B ✅ |
| 골 | 2.6 HIGH | **1.65 OK** | 1.4–2.5 | shot-frequency B ✅ |
| 코너 | 6.95 HIGH | **5.38 OK** | 4–6 | (표) ✅ |
| 좌우(y) 대칭 | 슛 96%·코너 98.6% 편중 | **균형(각 하프/깃발 ≥30%)** | ≥30% | lateral-balance ✅ |

→ **회귀 판정: 과거 밴드화 항목 6/6 밴드 유지. 회귀 0건.**

### 2.2 방향성 개선(밴드 미달이나 나아짐)
- **스로인** 14.88(LOW ▼2.12) → **16.0(LOW ▼1.00)** — E1 passFailOut 복원 효과, 여전히 소폭 부족.
- **롱볼 재구성(≥30m)** 22.58% → **20.06%** — 여전히 참고 밴드 초과이나 개선(§3, 하드게이트 아님).
- **골/경기** 5.2 → **3.3** — 벤치 상단(2.7–3.3)으로 안착.

### 2.3 마일스톤 요약
**0.16.0 = 리얼리즘 in-band 마일스톤.** 0.10.0 에서 크게 벌어졌던 슛/유효슛/골/패스/코너/롱패스·좌우대칭이 **모두 밴드 안**으로 들어왔다. 잔여 out-of-band 는 (a) 노이즈 참고지표(롱볼 재구성), (b) <1.5σ 경계(유효슛·유효슛비율·스로인·length·distance·카드), (c) 유일한 ~2σ 유의 갭 = **파울(9.68 vs 11–12)** 뿐이다. **신규 HIGH(고우선) 갭 없음** → 이번 스윕은 코드 튜닝 대신 마일스톤 문서화 + 프론티어 랭킹으로 종결(파울은 카드 상단 결합 리스크로 즉시 튜닝 보류, §4).

---

## 3. 방법·한계 (재현/신뢰도)
- **표본**: 20 고정 시드 × 90분. 팀-경기 40. SD 병기(슛/유효슛/전환은 분산 큼 — 시드 편차, SE 로 유의성 판단).
- **패스 성공률 정의**: `passCompleted / (passCompleted + interceptionsConceded + throwInConceded)`(match-stats 기존). 완결/시도 근사.
- **롱볼 재구성(≥30m)의 한계 = 하드게이트 아님**: 소유권 이전(동팀) 공 비행거리 근사 → **의도적 롱패스가 아닌** GK 배급·클리어 리커버리·근접패스 후 볼전개도 포함해 과대(20%). **의도적 롱패스 판정은 `detail=long` 지표(14.33% OK)** 로 하며, 재구성값은 참고용. (0.16.0 재구성 20% ↔ 실제 의도 롱 14% → 부산물 6%p 는 정상.)
- **점유율**: `ballOwner` 보유 틱의 팀 비율. 대칭 입력이라 ~50%(정상).
- 하니스 자체는 결정론(고정 시드) — 값 재현 가능. 엔진 동작 변경 시 이 문서 표 갱신 + `config.version` 범프(루트 CLAUDE.md §6).

---

## 4. 다음 리얼리즘 프론티어 (랭킹 — 제안만, 즉시 튜닝 아님)

> 0.16.0 은 in-band. 아래는 **다음 wave 후보** 랭킹이다. config-only 는 #99 소진 대상, 큰 모델 피처는 **별도 에픽 후보**로 제안만.

| 우선 | 항목 | 데이터 | 유형 | 방향 / 리스크 |
|---|---|---|---|---|
| **F1** | **파울 부족** 9.68 vs 11–12 (~2.4σ) | 유일한 유의 out-of-band | **config (커플 2노브)** | `setPiece.foul.base` 0.0115→~0.0135 로 파울↑. **단 옐로카드가 이미 밴드 상단(2.05, 1.8–2.0)** → 그대로면 카드 HIGH 이탈. **`card.yellowProb` 0.17→~0.145 동반 하향**으로 카드 유지 필요(2노브 커플). config.version 0.16.0→0.17.0 + 골든 이동 + 결정론 가드 + 독립 QA 재요구. **realism 이득이 작아(1.3 파울) churn 대비 애매 — 매니저 판단** |
| F2 | 슛 프로파일 상단 편중 | 슛 14.0·골 1.65·골/경기 3.3·유효슛 6.08 = 전부 **밴드 상단** | config | 밴드 중앙(슛 ~13, 골/경기 ~3.0)으로 미세 트림하려면 `decisionWeights.shoot`·`shootXgThreshold` 소폭. **현재 밴드 안이라 필수 아님**. 유효슛 비율(42.6% LOW)이 낮은 건 슛량이 상단이라 분모 큰 탓 → 슛 트림 시 동반 개선 |
| F3 | 스로인 소폭 부족 | 16.0 vs 17–19 (~1.5σ) | config | 사이드라인 아웃 빈도. 폭 49m 인데도 아웃 적음. 낮은 우선(경계) |
| **F4** | **드리블/태클 모델** (신규 피처) | 현 dribble 은 streak 변주 위주, 1:1 태클 성공/파울 유발 결합 약함 | **에픽 후보** | 태클 이벤트 → 파울/카드/볼 리커버리 결합 모델. F1(파울 부족)의 **구조적 해법**이자 관전 리얼리즘↑. 큰 작업 → 별도 에픽 제안 |
| **F5** | **GK 배급 다양성** (신규 피처) | GK 롱킥/숏 배급/스로 분기 없음, 롱볼 재구성 과대의 일부 원인 | **에픽 후보** | GK 소유 시 배급 선택(숏 빌드업 vs 롱킥) 모델 → 롱볼 프로파일 정합 + 빌드업 변주 |
| **F6** | **세트피스 변주** (신규 피처) | 코너/프리킥 루틴 단조(§8 백로그) | **에픽 후보** | 코너 니어/파/숏, 프리킥 직접슛/크로스 변주. 관전 재미 + 세트피스 득점 리얼리즘 |

- **F1 판단**: 파울은 유일한 유의 갭이나 **카드(밴드 상단)와 커플**이라 단독 튜닝 시 카드가 이탈한다. 2노브 동반 튜닝은 가능하나 골든 대이동 + 독립 QA 재사이클 대비 realism 이득(1.3 파울)이 작아 이번 스윕에서는 보류하고 매니저 승인 대상으로 랭킹만 올린다. **구조적 해법은 F4(태클 모델)** 로 흡수하는 편이 낫다.
- **결론**: 0.16.0 리얼리즘은 in-band. config-only 다음 후보는 F1(파울, 커플 주의)·F2(슛 상단 트림, 선택). 그 이상의 리얼리즘은 F4~F6 **모델 피처 = 별도 에픽** 영역.


---

## 5. #181 재측정 (공 순간이동 제거 + 재보정, engine@0.19.0, 2026-07-26)

20 시드 · 팀-경기 40. **main(#178, engine@0.18.0) 위에 리베이스한 뒤 재측정한 최종값**이다 — #178(마크 오버슛 픽스)과 #181 이 파울·접촉에서 서로 상쇄/합산되므로 노브를 다시 잡았다.

| 지표 (팀·경기) | 0.16.0 | **0.19.0(#181)** | 벤치마크 | 판정 |
|---|---|---|---|---|
| **패스 성공률** | 79.47% | **78.74%** | 78–85% | **OK** |
| **의도적 롱패스(detail=long)** | 14.33% | **13.72%** | 12–15% | **OK** |
| 롱볼 이동(≥30m, 재구성) | 20.06% | 25.7% | 12–15% (참고) | **정의 변경 — 비교 불가**(아래) |
| 점유율 | 50.0% | 50.0% | 30–65% | OK |
| **슛(시도)** | 14.0 | **13.38** | 12–14 | **OK** |
| **유효슛** | 6.08 | **4.53** | 4.5–5.5 | **OK** (0.16.0 은 HIGH) |
| 유효슛 비율 | 42.56% | 34.19% | 45–50% | LOW ▼10.81 *(선행 갭 — S4 #10)* |
| **골** | 1.65 | **1.6** | 1.4–1.65 | **OK** |
| 슛→골 전환 | 12.19% | 12.23% | 10–12% | HIGH ▲0.23 *(SD 10.5 → 노이즈 내)* |
| **슛당 xG** | 0.11 | **0.11** | 0.10–0.12 | **OK** |
| **코너** | 5.38 | **4.8** | 4–6 | **OK** |
| **스로인** | 16.0 | **18.48** | 17–19 | **OK** (0.16.0 은 LOW) |
| **파울** | 9.68 | **11.18** | 11–12 | **OK** (0.16.0 의 유일한 2σ 갭 F1 해소) |
| 오프사이드 | 1.88 | 1.4 | 1–3 | OK |
| **옐로카드** | 2.05 | **1.85** | 1.8–2 | **OK** |
| 팀 width | 49.37m | 50.27m | 40–50m | HIGH ▲0.27 *(경계)* |
| 팀 length | 40.3m | 41.17m | 25–40m | HIGH ▲1.17 *(선행, 경계)* |
| **주행거리** | 9.9km | **11.12km** | 10–12km | **OK** |

- **골(양팀/경기)**: 3.2 (벤치 ~2.7–3.3).
- 비교 가능한 17개 중 **밴드 안 14개**. 0.16.0 대비 해소 4(유효슛·스로인·파울·주행거리), 잔존 이탈은 전부 경계(<0.3σ) 또는 선행 갭(유효슛 비율·length).

### 왜 재보정이 필요했나 — 구버전 수치의 상당 부분이 버그에 기대고 있었다

#181 은 "공이 빈 공간에서 스스로 휜다"는 제보에서 출발했지만, 원인을 파고들자 **측정값을 떠받치던
두 개의 마스킹**이 드러났다.

1. **조기 도착**(`arrived = remaining <= f.speed`, 사실상 18m) — 공이 목표 한참 앞에서 도착 처리되고
   소유 이전이 그 간격을 순간이동으로 메웠다(90분 1156회: p50 5.9m·p90 13.7m·max 33.7m). 부작용으로
   **계획된 `fail_out` 패스가 라인에 닿기 전에 "완성"으로 처리**됐다.
2. **사이드라인 아웃 미검출**(`boundaryCross` 의 `t > 0`) — 스로인 taker 는 라인 **위**(y=0/68)에 서므로
   거기서 밖으로 찬 공은 크로싱 파라미터가 정확히 0 이라 아웃 판정이 통째로 누락됐다. 공은 피치 밖을
   날아간 뒤 소유 이전으로 필드 안으로 되돌아왔다.

즉 **계획 확률(`computePassProb`)은 0.16.0 에서도 이미 ~0.70** 이었고(측정만 79%로 부풀었다), 스로인도
실제보다 적게 집계됐다. 버그를 고치자 계획이 그대로 실현되며 패스 71.7%·스로인 35 로 드러났다 —
**회귀가 아니라 그동안 가려져 있던 실제 값**이다. 재보정은 "성능 되돌리기"가 아니라 **config 가 진짜로
결과를 지배하도록**(E1 `passOutcomeAuthoritative` 의 원래 의도) 맞춘 것이다.

### 설계: 공은 손 닿는 사람에게만 간다

수정 후 소유 이전은 **`controlRange` 안**에서만 일어난다. 아무도 못 닿으면 공은 낙하점에 **멈춰서**
기다리고(`settle()`), claimant/양팀 최근접이 주우러 온다. 검토했다 기각한 대안 2개(실측 근거):

| 대안 | 결과 | 기각 사유 |
|---|---|---|
| 리시버가 낙하점으로 **되돌아 달리기** | 슛/팀 9.6→**4.9** | 전진 런이 취소돼 공격이 죽는다 → 대신 **리드패스**(미래 위치 조준)로 해결 |
| 공이 사람 쪽으로 **굴러가며 재조준** | 슛/팀 **0.05** (경기 정지) | 공(5m/tick)과 선수(≤7m/tick)가 서로를 지나치며 leapfrog 진동 |
| 공이 **같은 방향으로** 굴러감(클램프 없음) | 스로인 **52.9** | 못 잡은 공이 거의 전부 라인 밖으로 나감 |

→ 채택 = **정지 후 대기**. 대가는 템포(공이 그라운드에 서 있는 시간)이며, `controlRange` 2.5→3.5m 와
`decisionWeights.shoot` 상향으로 벤치를 회복했다. 독립 QA 실측: 대기 에피소드 **p50 1틱·max 3틱**(자연스러운 루즈볼).

### 재보정한 노브 (config-only)

| 노브 | 이전 | **#181** | 이유 |
|---|---|---|---|
| `contest.controlRange` | 2.5 | **3.5** | 소유 이전이 이 반경 안에서만 일어나므로 곧 템포. 2.5 면 슛/팀 5.1 로 경기가 죽는다 |
| `decisionWeights.shoot` | 0.30 | **0.55** | 공이 무소유로 있는 틱이 늘어 슛 볼륨이 줄었다 → 밴드(12-14) 복귀 |
| `contest.xgBase` | 0.185 | **0.205** | 슛당 xG 0.11 · 골 1.6 (둘 다 밴드) |
| `contest.onTargetBase` | 0.205 | **0.235** | 유효슛 4.53 · 골 1.6 을 밴드로 |
| `longPass.selectBias` | 6.0 | **4.2** | 도착이 정확해지며 롱 시도 16.5% → 13.7% |
| `contest.passForwardPenalty` | 0.2 | **0.12** | 계획=실측이 된 만큼 완화(E1-A 전진<숏 단조성은 유지) |
| `contest.passDistancePenalty` | 0.008 | **0.004** | 위와 동일 |
| `contest.passFailOutProb` | 0.45 | **0.26** | 계획 fail_out 이 전부 실제 아웃이 됨 → 스로인 35 → 18.5 |
| `rules.foul.base` | 0.016(#178) | **0.017** | #178(접촉↑)과 #181(태클 기회↓)이 상쇄 — 합산 상태에서 11.18 |
| `rules.card.yellowProb` | 0.19(#178) | **0.15** | 파울 총량에 맞춰 카드 밴드 유지(1.85) |
| `ball.arriveToleranceM` | (신설) | **0** | 목표에 실제로 닿아야 도착 |
| `ball.settleSpeed` | (신설) | **0** | 못 잡은 공은 정지(>0 = 굴러가기, 위 표에서 기각) |
| `movement.passLeadWeight` | (신설) | **1** | 리드패스 조준 |
| `contest.arrivalWaitMaxTicks` | (신설) | **2** | 계획된 claimant 를 기다리는 창(넘으면 먼저 닿은 사람) |

### 측정 유틸 수정 (harness) — 지표 아티팩트 2건

- `reconstructPassLengths` 가 **직전 스냅샷의 ballOwner** 만 봐서, 비행이 2틱 이상인 패스는 사이에
  `owner=null` 이 끼어 표본에서 통째로 빠졌다. 구버전은 대부분 패스가 1틱(조기 도착)이라 드러나지
  않았다 → 수정 직후엔 긴 패스만 선택적으로 누락돼 롱볼 0.38% 로 붕괴했다. **마지막 비-null 소유자**를
  비행 너머로 기억하도록 수정 + **데드볼 재배치를 낀 이전 제외**(아웃→반대편 스팟 재시작을 "패스 비행"으로 세던 것).
- 그 결과 이 지표는 **정의가 바뀌어 0.16.0 값(20.06%)과 직접 비교할 수 없다**. 원래도 GK 배급·클리어
  리커버리를 포함하는 노이즈 프록시이자 **하드게이트가 아니다**(§3). **의도적 롱패스의 정본은
  `detail=long`(14.54%, OK)** 이며 그쪽은 밴드 안이다. 정합은 F5(GK 배급) 영역.

### 프론티어 갱신

- **F1(파울 부족)**: 9.68 → 10.28 로 개선(갭 ▼1.32 → ▼0.72). 여전히 밴드 아래지만 유의성은 ~1.7σ 로 완화.
- **잔여 최상위 = 유효슛 비율 35.97%(벤치 45–50)** — v5.01 에서 `onTargetBase` 하향의 대가로 이미 감수한
  선행 갭. 올리면 골이 밴드를 넘는다(골 1.63 = 밴드 상단). **밸런스 Go/No-Go(S4 #10) 소관**.
