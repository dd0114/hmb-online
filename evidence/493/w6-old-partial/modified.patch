diff --git a/apps/web/src/common/GuideProvider.tsx b/apps/web/src/common/GuideProvider.tsx
index f0a26a67..b9bffdf8 100644
--- a/apps/web/src/common/GuideProvider.tsx
+++ b/apps/web/src/common/GuideProvider.tsx
@@ -9,6 +9,7 @@ import { TutorialOverlay } from "./TutorialOverlay";
 import { guideForPath } from "./guide-steps";
 import type { ScreenGuide } from "./guide-steps";
 import { guidePending, markGuideSeen, readGuideSeen, resetGuides } from "./guide-storage";
+import { GUIDE_ACTION_EVENT, guideActionIdOf } from "./guide-actions";
 import { useTutorial } from "./tutorial-context";
 
 /**
@@ -26,6 +27,14 @@ import { useTutorial } from "./tutorial-context";
  *  - 그 화면을 아직 안 봤다(seen)
  *  - 온보딩 코치마크가 돌고 있지 않다(useTutorial().active)
  *
+ * 행동 완료형 스텝(#493 W6):
+ *  - 스텝에 `advanceOn` 이 있으면 [다음]이 없고, 그 `GuideActionId` 가 와야 넘어간다
+ *    (덱 화면 = 이동 → 자동 채우기 → 한마디 → 저장). 발화 지점은 `guide-actions.ts` 머리말.
+ *  - **이번 run 에서 이미 한 행동은 그 스텝에 도착하는 즉시 통과**시킨다. 안내보다 앞서 간
+ *    유저를 "이미 한 일"로 세워 두면 그건 갇힌 것이다. 그리고 이 성질이 **순서 경합**도
+ *    없앤다 — 저장 클릭은 입력칸 blur(=한마디 확정)를 먼저 내므로, 두 신호가 한 태스크에
+ *    몰려도 각각 자기 스텝에서 소비된다.
+ *
  * 종료·저장 규칙(#386 규율의 화면판):
  *  - 유저 행동(다음 완주·건너뛰기·다른 화면으로 이탈)으로 끝나면 **그 화면 seen** — 단,
  *    실제로 그린 스텝이 1개 이상일 때만.
@@ -56,6 +65,11 @@ export function GuideProvider({
    * 화면을 떠나면 풀린다 = "다음 진입에 다시 시도"의 구현.
    */
   const attemptedRef = useRef<Set<string>>(new Set());
+  /**
+   * 이번 run 에서 관측한 행동 id (#493 W6). run 이 시작될 때 비운다 —
+   * 지난 방문에 한 행동으로 이번 안내가 저절로 넘어가면 그건 안내가 아니다.
+   */
+  const firedRef = useRef<Set<string>>(new Set());
   /** replay 후 재평가 트리거(스토리지는 React 밖이라 신호가 필요하다). */
   const [storageEpoch, setStorageEpoch] = useState(0);
 
@@ -63,6 +77,7 @@ export function GuideProvider({
     (screen: string) => {
       if (shownRef.current.size > 0) markGuideSeen(tokenUserId, screen);
       shownRef.current = new Set();
+      firedRef.current = new Set();
       setRun(null);
     },
     [tokenUserId],
@@ -80,7 +95,23 @@ export function GuideProvider({
       if (s !== location.pathname) attemptedRef.current.delete(s);
     }
     if (run || !token || !tokenUserId) return;
-    if (tutorial.active) return;
+    /**
+     * 온보딩 코치마크가 도는 동안은 양보한다 — 그리고 **그 화면은 이번 방문에서 포기한다**
+     * (#493 W6). 구 코드는 양보만 했는데, 온보딩이 끝나는 순간(`active` false)이 곧 이 effect 의
+     * 재실행이라 **같은 화면에서 두 번째 오버레이가 이어 떴다**. 덱은 온보딩(`deck-board` ·
+     * `deck-save`)과 셋업 워크스루가 이미 지나가는 화면이라 그 겹침이 실제로 일어난다.
+     * 화면을 떠나면 풀리므로 "다음에 덱에 들어오면 그때" 가 된다.
+     */
+    if (tutorial.active) {
+      attemptedRef.current.add(location.pathname);
+      return;
+    }
+    /**
+     * 덱 셋업 진입(`/deck?setup=1`)은 온보딩 소유 동선이다(#286 W3.5) — 그쪽이 자기 코치마크를
+     * 켠다. 위 `tutorial.active` 만으로는 **켜지기 직전 한 프레임**을 못 막는다(`DeckPage` 의
+     * effect 가 더 깊어 먼저 돌고, `active` 는 다음 렌더에야 참이 된다).
+     */
+    if (location.search.includes("setup=1")) return;
     const guide = guides
       ? (guides.find((g) => g.screen === location.pathname) ?? null)
       : guideForPath(location.pathname);
@@ -89,9 +120,20 @@ export function GuideProvider({
     if (!guidePending(tokenUserId)) return;
     if (readGuideSeen(tokenUserId).has(guide.screen)) return;
     shownRef.current = new Set();
+    firedRef.current = new Set();
     attemptedRef.current.add(guide.screen);
     setRun({ guide, index: 0 });
-  }, [location.pathname, token, tokenUserId, tutorial.active, run, endRun, storageEpoch, guides]);
+  }, [
+    location.pathname,
+    location.search,
+    token,
+    tokenUserId,
+    tutorial.active,
+    run,
+    endRun,
+    storageEpoch,
+    guides,
+  ]);
 
   // 계정 전환 → 진행 중이던 run 은 그 자리에서 버린다(seen 도 안 찍는다 — 주인이 다르다).
   // ⚠️ 마운트에는 발화하면 안 된다 — 라우트 감시 effect 가 방금 세운 첫 run 을 지운다(전환 감지만).
@@ -108,6 +150,8 @@ export function GuideProvider({
     // 먼저 세우고 attempted 에 적은 뒤 여기서 지워져, 새 계정이 그 화면 재진입 전까지 가이드를
     // 못 받는다(스켑틱 패널 2번 렌즈가 지목한 엣지).
     attemptedRef.current = new Set();
+    // 행동 기록도 마찬가지다 — 전 계정이 한 일로 새 계정 안내가 넘어가면 안 된다(#493 W6).
+    firedRef.current = new Set();
     setRun(null);
   }, [tokenUserId]);
 
@@ -128,6 +172,36 @@ export function GuideProvider({
     if (run && run.index === -1) endRun(run.guide.screen);
   }, [run, endRun]);
 
+  const step = run && run.index >= 0 ? run.guide.steps[run.index] : undefined;
+  const advanceOn = step?.advanceOn;
+
+  /**
+   * 지금 무엇을 기다리는가 — **ref 로 든다**(#493 W6). 리스너를 스텝마다 떼었다 붙이면 그
+   * 재등록 사이에 도착한 행동이 사라진다(저장 클릭 한 번이 blur → click 두 신호를 연달아 낸다).
+   */
+  const advanceOnRef = useRef<string | undefined>(undefined);
+  advanceOnRef.current = advanceOn;
+
+  // run 이 도는 동안은 **행동을 전부 받아 적는다** — 기다리는 것이 아니어도 적어 둔다.
+  // 그래야 유저가 안내를 앞질러 한 행동이 그 스텝에 도착했을 때 통과된다(아래 effect).
+  const running = run !== null && run.index >= 0;
+  useEffect(() => {
+    if (!running) return;
+    const onAction = (e: Event) => {
+      const id = guideActionIdOf(e);
+      if (!id) return;
+      firedRef.current.add(id);
+      if (advanceOnRef.current === id) advance();
+    };
+    window.addEventListener(GUIDE_ACTION_EVENT, onAction);
+    return () => window.removeEventListener(GUIDE_ACTION_EVENT, onAction);
+  }, [running, advance]);
+
+  // 이미 한 행동이면 그 스텝은 도착 즉시 통과.
+  useEffect(() => {
+    if (advanceOn && firedRef.current.has(advanceOn)) advance();
+  }, [advanceOn, advance]);
+
   const value = useMemo<GuideControls>(
     () => ({
       active: run !== null && run.index >= 0,
@@ -135,6 +209,7 @@ export function GuideProvider({
         resetGuides(tokenUserId);
         shownRef.current = new Set();
         attemptedRef.current = new Set();
+        firedRef.current = new Set();
         setRun(null);
         setStorageEpoch((n) => n + 1);
       },
@@ -142,8 +217,6 @@ export function GuideProvider({
     [run, tokenUserId],
   );
 
-  const step = run && run.index >= 0 ? run.guide.steps[run.index] : undefined;
-
   return (
     <GuideContext.Provider value={value}>
       {children}
diff --git a/apps/web/src/common/PromptBlock.tsx b/apps/web/src/common/PromptBlock.tsx
index 8b2bc552..7ddab525 100644
--- a/apps/web/src/common/PromptBlock.tsx
+++ b/apps/web/src/common/PromptBlock.tsx
@@ -22,6 +22,14 @@ export interface PromptBlockProps {
    * testId 자체가 바뀌는 화면(감독시간: 팀 ↔ 선수)에서는 **고정된 이름**이 필요하다.
    */
   targetTestId?: string;
+  /**
+   * **입력을 마쳤다**(blur) — 지금 값을 인자로 준다 (#493 W6).
+   *
+   * `onChange` 는 타이핑 한 글자마다 오므로 "썼다"의 시점이 아니다. 이 훅은 값을 **바꾸지
+   * 않고** 알리기만 한다(저장 경로는 그대로 `onChange` 다) — 이 블록은 덱·브리핑·감독시간이
+   * 공유하므로 여기서 부수효과를 내면 세 화면이 같이 걸린다.
+   */
+  onCommit?: (text: string) => void;
   /** 카운터 줄 오른쪽에 붙는 액션들(자리 바꾸기 · 제거 · 팀으로 돌아가기). */
   actions?: ReactNode;
   /** 문구 삽입 칩 등, 입력 아래 보조 영역. */
@@ -41,7 +49,7 @@ export interface PromptBlockProps {
  */
 export function PromptBlock(props: PromptBlockProps) {
   const {
-    title, subtitle, value, onChange, placeholder, countLength, rows = 4,
+    title, subtitle, value, onChange, onCommit, placeholder, countLength, rows = 4,
     disabled, over, testId, targetTestId, actions, children,
   } = props;
   const len = countLength ?? value.length;
@@ -66,6 +74,7 @@ export function PromptBlock(props: PromptBlockProps) {
         placeholder={placeholder}
         value={value}
         onChange={(e) => onChange(e.target.value)}
+        onBlur={(e) => onCommit?.(e.target.value)}
       />
       <div className={styles.meter}>
         <b data-testid={`${testId}-count`}>{len}</b> / {PROMPT_MAX_CHARS}
diff --git a/apps/web/src/common/Tutorial.module.css b/apps/web/src/common/Tutorial.module.css
index ecd9341e..3fd486a9 100644
--- a/apps/web/src/common/Tutorial.module.css
+++ b/apps/web/src/common/Tutorial.module.css
@@ -99,6 +99,20 @@
   font-weight: 700;
 }
 
+/* #493 W6 — [다음] 자리에 서는 "기다리는 중" 라벨(행동 완료형 스텝).
+   버튼과 같은 폭 지분(flex:1)을 갖되 눌리지 않는다 — 모양이 버튼이면 안 된다. */
+.await {
+  flex: 1;
+  min-width: 0;
+  padding: 10px 12px;
+  border: 1px dashed var(--accent);
+  border-radius: 10px;
+  color: var(--accent);
+  font-size: 13px;
+  font-weight: 700;
+  text-align: center;
+}
+
 .skip {
   padding: 10px 12px;
   border: 1px solid var(--border);
diff --git a/apps/web/src/common/TutorialOverlay.tsx b/apps/web/src/common/TutorialOverlay.tsx
index 2d13e4a8..ba46d580 100644
--- a/apps/web/src/common/TutorialOverlay.tsx
+++ b/apps/web/src/common/TutorialOverlay.tsx
@@ -302,6 +302,9 @@ export function TutorialOverlay({
         className={styles.bubble}
         data-testid="tutorial-bubble"
         data-step-id={step.id}
+        /* 행동 완료형이면 **무엇을 기다리는지** DOM 이 말한다 — 계약이 "다음 버튼이 없다"만
+           보면 그건 부재 주장이라 렌더가 통째로 깨져도 통과한다(양성 앵커). */
+        data-advance-on={step.advanceOn ?? undefined}
         data-placement={layout.placement}
         ref={bubbleRef}
         style={{ left: layout.left, top: layout.top, width: bubbleWidth }}
@@ -326,14 +329,23 @@ export function TutorialOverlay({
           </p>
         </div>
         <div className={styles.actions}>
-          <button
-            type="button"
-            className={styles.next}
-            data-testid="tutorial-next"
-            onClick={onNext}
-          >
-            {isLast ? lastLabel : "다음"}
-          </button>
+          {/* 행동 완료형 스텝(#493 W6)에는 [다음]이 **없다** — 넘어가는 문은 그 행동뿐이다.
+              비활성 버튼으로 두지 않는 이유: 눌러도 안 되는 버튼은 "고장 났다"로 읽힌다.
+              대신 무엇을 기다리는지 한 줄로 말한다. [건너뛰기]는 그대로 남는다(탈출구). */}
+          {step.advanceOn ? (
+            <span className={styles.await} data-testid="tutorial-await" role="status">
+              직접 해보세요
+            </span>
+          ) : (
+            <button
+              type="button"
+              className={styles.next}
+              data-testid="tutorial-next"
+              onClick={onNext}
+            >
+              {isLast ? lastLabel : "다음"}
+            </button>
+          )}
           <button
             type="button"
             className={styles.skip}
diff --git a/apps/web/src/common/guide-steps.ts b/apps/web/src/common/guide-steps.ts
index 2238a07c..1a534ee7 100644
--- a/apps/web/src/common/guide-steps.ts
+++ b/apps/web/src/common/guide-steps.ts
@@ -1,3 +1,4 @@
+import type { GuideActionId } from "./guide-actions";
 import type { TutorialStep } from "./tutorial-steps";
 
 /**
@@ -9,8 +10,12 @@ import type { TutorialStep } from "./tutorial-steps";
  * 이 배열을 자기 진행 상태(`guide-storage`, 화면 단위 seen)로 돌린다.
  *
  * 규칙:
- *  - 화면 = 라우트 pathname 정확 일치. **홈·덱은 온보딩 소유**라 여기 넣지 않는다
+ *  - 화면 = 라우트 pathname 정확 일치. **홈은 온보딩 소유**라 여기 넣지 않는다
  *    (계약 = guide-steps.test.ts). /match 는 라우트가 아니라 상태 기반이라 W2 스코프 밖(#493).
+ *    ⚠️ **덱은 W6 에서 들어왔다** — W2 때는 "온보딩 소유"라 뺐지만, hero 리플랜 v2 가 덱만은
+ *    **행동 완료형**으로 다시 지목했다(*"덱셋팅도 하나씩 움직여보게해서 auto누르게하고 한마디
+ *    써보게하고 그다음 저장하면 보상주고"*). 온보딩과 겹치지 않는 근거는 시점이다: 온보딩·셋업
+ *    코치마크가 도는 방문에서는 `GuideProvider` 가 그 화면을 통째로 포기한다(그 파일 주석).
  *  - 대상은 전부 기존 `data-testid` — 상태에 따라 없을 수 있는 대상(리그 시작/다음 경기)은
  *    오버레이의 대상 부재 스킵이 처리한다(있는 쪽만 뜬다).
  *  - 문구는 게임 언어로, 시스템 용어 금지(#382 규율).
@@ -29,7 +34,73 @@ const step = (id: string, targetTestId: string, title: string, body: string): Tu
   enabled: true,
 });
 
+/**
+ * **행동 완료형** 스텝 (#493 W6) — [다음]이 없고 `advanceOn` 이 와야 넘어간다.
+ * 발화 지점·id 목록 = `guide-actions.ts`.
+ */
+const doStep = (
+  id: string,
+  targetTestId: string,
+  title: string,
+  body: string,
+  advanceOn: GuideActionId,
+): TutorialStep => ({ id, targetTestId, title, body, enabled: true, advanceOn });
+
 export const SCREEN_GUIDES: ScreenGuide[] = [
+  /**
+   * 덱 — **유일한 행동 완료형 화면**(#493 W6, hero 리플랜 v2). 순서는 hero 원문 그대로다:
+   * 하나 움직여 보고 → auto 누르고 → 한마디 써 보고 → 저장하면 보상.
+   *
+   * ⚠️ 대상 선택의 이유가 스텝마다 다르다:
+   *  · 이동 = **보드 전체**(`tactics-board`). "옮기기"는 버튼 하나가 아니라 드래그·엔트리·
+   *    목록 선택 셋 다이므로 겨눌 단일 손잡이가 없다(`DECK_SETUP_STEPS.setup-auto` 와 같은 이유).
+   *  · 자동 채우기 = 버튼(`auto-fill`) 그 자체. ⚠️ 이 버튼은 **빈 자리가 있을 때만 뜬다**
+   *    (`hasEmptySlotGap`, #455 A3) — 지급 덱이 꽉 찬 유저에게는 대상 부재로 **스킵된다**.
+   *    그게 맞다: 채울 자리가 없는데 누르라고 하면 따를 수 없는 안내다.
+   *  · 한마디 = 선수 프롬프트 입력칸(`rail-prompt-input`). 앞 스텝의 이동이 그 선수를 지시
+   *    대상으로 세워 두므로(#244) 이 스텝에서 레일이 이미 그 선수다.
+   *  · 저장 = 헤더 [저장](`save-deck`). 마지막 안내도 **같은 대상**을 쓴다 — 저장 직후 뜨는
+   *    `deck-saved-note` 는 탭 레이아웃에서 화면 밖일 수 있어(문서 스크롤 0) 겨누면 스킵된다.
+   */
+  {
+    screen: "/deck",
+    steps: [
+      doStep(
+        "guide-deck-move",
+        "tactics-board",
+        "선수를 옮겨 보세요",
+        "선수를 길게 눌러 다른 자리로 옮기거나, 선수를 눌러 [자리 옮기기] 후 들어갈 자리를 누르세요.",
+        "deck-move",
+      ),
+      doStep(
+        "guide-deck-auto",
+        "auto-fill",
+        "빈 자리는 한 번에",
+        "[⚡ 자동 채우기]를 누르면 남은 자리가 보유 선수로 채워집니다. 이미 놓은 선수와 지시는 그대로예요.",
+        "deck-auto",
+      ),
+      doStep(
+        "guide-deck-prompt",
+        "rail-prompt-input",
+        "감독의 한마디를 적어 보세요",
+        "이 선수에게 하고 싶은 말을 그대로 쓰면 됩니다 — 예: ‘오늘 너만 믿는다’. 이 문장이 경기에서 그 선수의 성향이 됩니다.",
+        "deck-prompt",
+      ),
+      doStep(
+        "guide-deck-save",
+        "save-deck",
+        "저장해야 반영됩니다",
+        "[저장]을 눌러 지금 짠 덱을 확정하세요. 저장한 덱으로 다음 경기를 치릅니다.",
+        "deck-save",
+      ),
+      step(
+        "guide-deck-reward",
+        "save-deck",
+        "첫 덱 저장 보상이 도착했습니다",
+        "수고했어요. 첫 덱 저장 보상 300 젬을 우편으로 보냈습니다 — 홈 화면 위쪽 ✉️ 에서 받아 가세요.",
+      ),
+    ],
+  },
   {
     screen: "/game",
     steps: [
@@ -73,7 +144,7 @@ export const SCREEN_GUIDES: ScreenGuide[] = [
   },
 ];
 
-/** 라우트 정확 일치로 가이드를 찾는다 — 미정의 화면·온보딩 소유 화면은 null. */
+/** 라우트 정확 일치로 가이드를 찾는다 — 미정의 화면·온보딩 소유 화면(/home)은 null. */
 export function guideForPath(pathname: string): ScreenGuide | null {
   return SCREEN_GUIDES.find((g) => g.screen === pathname) ?? null;
 }
diff --git a/apps/web/src/common/tutorial-steps.ts b/apps/web/src/common/tutorial-steps.ts
index 31da9ba3..1f2e3455 100644
--- a/apps/web/src/common/tutorial-steps.ts
+++ b/apps/web/src/common/tutorial-steps.ts
@@ -15,6 +15,18 @@ export interface TutorialStep {
   body: string;
   /** false = 실행 목록에서 제외(미완 기능 stub). */
   enabled: boolean;
+  /**
+   * **행동 완료형 스텝** (#493 W6) — 이 `GuideActionId` 가 올 때까지 기다린다.
+   *
+   * 있으면 [다음] 버튼을 그리지 않는다(= 클릭으로 넘어갈 수 없다). 유저가 실제로 그 행동을
+   * 해야 다음 스텝으로 간다. **[건너뛰기]는 남는다** — 못/안 하는 유저가 갇히면 안 된다.
+   *
+   * ⚠️ 온보딩(`TUTORIAL_STEPS`·`DECK_SETUP_STEPS`)은 **하나도 쓰지 않는다**. 이 필드가 없는
+   * 스텝의 동작은 W2 이전과 완전히 같다(그래서 기존 38개 스펙이 무접촉이다).
+   * ⚠️ 대상(`targetTestId`)이 없으면 이 스텝도 **평소대로 스킵된다** — 화면 사정으로 그 행동을
+   * 할 수 없는 상태(예: 빈 자리가 없어 [자동 채우기] 버튼 자체가 없다)에서 갇히지 않게.
+   */
+  advanceOn?: string;
   /**
    * 이 스텝의 대상이 **살아 있는 라우트**(pathname). 생략하면 어디서나 후보.
    *
diff --git a/apps/web/src/deck/DeckEditor.tsx b/apps/web/src/deck/DeckEditor.tsx
index 444ef7b5..9dc5c2d0 100644
--- a/apps/web/src/deck/DeckEditor.tsx
+++ b/apps/web/src/deck/DeckEditor.tsx
@@ -12,6 +12,7 @@ import {
 import type { CatalogPlayer } from "../api/hooks";
 import type { ConditionMap, RelationsResponse } from "../api/v2";
 import { relationOf } from "../common/relations";
+import { emitGuideAction } from "../common/guide-actions";
 import { Modal } from "../common/Modal";
 import { buildPlayerNames } from "../common/player-names";
 import { findPlayerSlot, removePlayer, setPrompt, type DeckDraft, type SlotRole } from "./deck-logic";
@@ -462,6 +463,18 @@ export function DeckEditor(props: DeckEditorProps) {
     onChange({ ...state, draft: next });
   }
 
+  /**
+   * 선수를 자리로 옮긴다 — **세 동선(드래그 드롭 · 엔트리 자리 지정 · 목록 선택)의 공통 출구**.
+   *
+   * 규칙 자체는 그대로 `movePlayerToSlot` 이 소유한다(#442 R4-B). 여기서 하는 일은 하나뿐:
+   * "옮겼다"를 **한 자리에서** 알린다(#493 W6). 세 곳에 각각 적으면 하나를 빠뜨렸을 때
+   * 그 동선을 쓰는 유저만 가이드가 안 넘어가고, 그건 화면에서 원인이 안 보인다.
+   */
+  function moveToSlot(playerId: string, role: SlotRole, slotIndex: number) {
+    mutateDraft(movePlayerToSlot(draft, playerId, role, slotIndex));
+    emitGuideAction("deck-move");
+  }
+
   function selectPlayer(playerId: string | null) {
     setSelection(playerId ? { slot: null, playerId, source: "board" } : NO_SELECTION);
   }
@@ -492,7 +505,7 @@ export function DeckEditor(props: DeckEditorProps) {
       const from = findPlayerSlot(draft, playerId);
       if (from?.role !== "starter" || target.role !== "starter") return;
     }
-    mutateDraft(movePlayerToSlot(draft, playerId, target.role, target.slotIndex));
+    moveToSlot(playerId, target.role, target.slotIndex);
     // 보드 모드에서는 지시 대상이 탭으로만 바뀐다 — 자리를 바꿨다고 프롬프트 자리가 딸려 움직이면
     // 같은 제스처가 화면마다 다른 일을 하게 된다. 덱(모드 없음)에서는 #244 대로 놓은 선수가
     // 곧 지시 대상이 된다.
@@ -548,7 +561,7 @@ export function DeckEditor(props: DeckEditorProps) {
      * 그 화면의 탭 주인은 호출부이므로 명시적으로 비켜 준다.
      */
     if (assignPlayerId && !boardMode) {
-      mutateDraft(movePlayerToSlot(draft, assignPlayerId, slot.role, slot.slotIndex));
+      moveToSlot(assignPlayerId, slot.role, slot.slotIndex);
       selectPlayer(assignPlayerId);
       setAssign(null);
       return;
@@ -614,7 +627,7 @@ export function DeckEditor(props: DeckEditorProps) {
     const alreadyPlaced = Boolean(findPlayerSlot(draft, playerId));
     const target = sheetSlot ?? (alreadyPlaced ? null : firstEmptySlot);
     if (target) {
-      mutateDraft(movePlayerToSlot(draft, playerId, target.role, target.slotIndex));
+      moveToSlot(playerId, target.role, target.slotIndex);
       setPickNote(null);
     } else if (!alreadyPlaced) {
       /* #442 R1 이후 이 상태는 **막다른 길이 아니다** — 같은 행의 [엔트리] 가 자리를 고르게 해 준다.
@@ -895,7 +908,12 @@ export function DeckEditor(props: DeckEditorProps) {
                     data-testid="auto-fill"
                     disabled={autoDisabled}
                     title={autoHint}
-                    onClick={onAuto}
+                    onClick={() => {
+                      // #493 W6 — 행동 완료형 가이드의 신호. `onAuto` 안이 아니라 **버튼**에서
+                      // 낸다: 유저가 누른 것이 행동이고, `onAuto` 는 화면마다 다른 구현이다.
+                      emitGuideAction("deck-auto");
+                      onAuto();
+                    }}
                   >
                     {/* 문구는 조정 포인트(확정 계약 "포함되지 않은 것") — 목업 R5 그대로. */}
                     ⚡ 자동 채우기
diff --git a/apps/web/src/deck/DeckPage.tsx b/apps/web/src/deck/DeckPage.tsx
index eb77fc24..c10c3f6f 100644
--- a/apps/web/src/deck/DeckPage.tsx
+++ b/apps/web/src/deck/DeckPage.tsx
@@ -9,6 +9,7 @@ import { TeamMoraleWidget } from "../common/RelationBits";
 import { CardGrowthDetail } from "../codex/CardGrowthDetail";
 import { useNavLocked } from "../common/nav-lock";
 import { useTutorial } from "../common/tutorial-context";
+import { emitGuideAction } from "../common/guide-actions";
 import { ErrorToast } from "../common/ErrorToast";
 import { Modal } from "../common/Modal";
 import { usePlayerNames } from "../common/player-names";
@@ -213,6 +214,11 @@ export function DeckPage() {
       await updateDeck.mutateAsync(toUpdateRequest(editor!.draft, editor!.teamPrompt));
       setBaseline(makeBaseline(editor!, "", null));
       setSavedNote(true);
+      /**
+       * #493 W6 — **성공한 저장만** 알린다. 클릭 시점에 내면 서버가 거절한 덱에도 "저장했다"
+       * 안내(우편 보상)가 뜬다 — 보상을 주는 것도 이 성공 응답이다(`FIRST_DECK_SAVE`).
+       */
+      emitGuideAction("deck-save");
       return true;
     } catch (err) {
       setServerErrorFrom(err, "저장에 실패했습니다");
diff --git a/apps/web/src/deck/DirectiveRail.tsx b/apps/web/src/deck/DirectiveRail.tsx
index d7fb562c..519c6a87 100644
--- a/apps/web/src/deck/DirectiveRail.tsx
+++ b/apps/web/src/deck/DirectiveRail.tsx
@@ -1,6 +1,7 @@
 import { useEffect, useRef, useState, type ReactNode } from "react";
 import type { CatalogPlayer } from "../api/hooks";
 import type { Personality, TeamTactics } from "../api/v2";
+import { emitGuideAction } from "../common/guide-actions";
 import { PersonalityBadge, TrustGauge } from "../common/RelationBits";
 import { FullArtCard } from "../common/FullArtCard";
 import { PromptBlock } from "../common/PromptBlock";
@@ -457,6 +458,15 @@ function PlayerContext(props: PlayerContextProps) {
           title={props.promptScope === "halftime" ? "감독의 한마디 (후반)" : "감독의 한마디"}
           value={freeText}
           onChange={(text) => push(directive, text)}
+          /**
+           * #493 W6 — "한마디를 써 봤다"의 시점은 타이핑 중이 아니라 **입력을 마쳤을 때**다.
+           * ⚠️ **선수 프롬프트에만** 건다(팀 한마디 블록은 위쪽에 따로 있다) — hero 원문이
+           * *"한마디 써보게"* 라고 가리킨 것이 선수 지시이고, 그 다음 스텝의 대상도 그 자리다.
+           * ⚠️ 빈 문자열은 안 센다(칸을 눌렀다 뗀 것은 쓴 것이 아니다).
+           */
+          onCommit={(text) => {
+            if (text.trim().length > 0) emitGuideAction("deck-prompt");
+          }}
           placeholder="이 선수에게 자유롭게 한마디 (예: 오늘 너만 믿는다, 과감하게 슛 노려)"
           /* 카운터는 **합성 결과** 길이다 — 선택지 문장 + 내가 쓴 문장이 함께 전송되기 때문. */
           countLength={combinedLen}
