# AC4 변이체 킬 — 읽는 법 (⚠️ 먼저 읽어라)

**`AC4-variant-kill-RAW-gate-disabled.log` 는 "실패해야 정상"인 로그다.** 그 파일만 열어서
`4 failed` 를 보고 "이 기능이 깨졌다"로 읽으면 정확히 거꾸로 읽은 것이다(2R 패널에서 실제로
한 번 그렇게 읽혔다 — 그래서 이 안내를 남긴다).

## 무엇을 한 것인가
계약이 **비어 있지 않음**을 증명하려고, 기제를 일부러 끄고 같은 e2e 를 돌렸다:

```
MaintenanceGate.tsx:  if (health === "outage")  →  if (false as boolean)
$ CI=1 WEB_E2E_PORT=5483 npx playwright test p477-maintenance --config=playwright.config.ts
(실행 후 `git checkout -- apps/web/src/common/MaintenanceGate.tsx` 로 원복,
 `grep -c 'health === "outage"'` → 2 로 원복 확인)
```

## 기대하는 뒤집힘과 실제 결과

| 파일 | 코드 상태 | 기대 | 실제 |
|---|---|---|---|
| `AC4-e2e.log` | **출하 코드 그대로** | 5 passed | **5 passed** |
| `AC4-variant-kill-RAW-gate-disabled.log` | **게이트를 끔(변이체)** | "뜬다" 4건 red · "안 뜬다" 1건 green | **4 failed / 1 passed** |

즉 두 로그는 **같은 코드의 두 실행이 아니라, 서로 반대인 두 코드의 실행**이다. 이 대조가
성립하므로 "뜬다" 계약 4건은 점검 화면의 실제 표시를 검사하고 있고(비어 있지 않고), 오탐
가드는 그것과 독립이다(기제를 꺼도 계속 통과 = 다른 것을 재고 있다).

## 살아남은 1건이 하필 오탐 가드인 이유
그 테스트의 단언은 **부재**("점검 화면이 뜨지 않는다")다. 기제를 끄면 당연히 계속 참이다.
그래서 이 한 건은 변이체 킬로 검정되지 않으며, 그 대신 출하 코드에서 **다운 시나리오 4건이
실제로 뜬다**는 사실이 그 테스트가 무의미하지 않음을 받쳐 준다.

---

# 두 번째 변이체 킬 — 라우트 전수 게이트 (3R)

`AC4-route-gate-variant-kill-RAW.log` = **같은 방식으로 `MaintenanceGate.tsx` 를 끈** 실행.

| 파일 | 코드 상태 | 기대 | 실제 |
|---|---|---|---|
| `AC1-AC2-unit.log` | 출하 코드 | 46 passed | **46 passed** |
| `AC4-route-gate-variant-kill-RAW.log` | 게이트를 끔 | outage 15건 red · ok 15건 green | **15 failed / 15 passed** |

`MaintenanceGate.test.ts` 는 **라우트 15개 × (outage · ok)** 를 돈다. 표본을 늘린 것이 아니라
성질을 바꾼 것이다 — outage 에서는 어떤 URL 이든 **라우터 트리가 통째로 렌더되지 않는다**는
구조를 검정하므로, 새 라우트가 추가돼도 이 성질이 따라온다.

---

# 캡처 대조군 — "한글이 깨졌다" 반박의 처리 (3R)

패널 1R·2R 이 연속으로 *"캡처 PNG 의 한글이 깨졌다"* 고 반박했다. 그런데 **두 반박이 서로 다른
깨진 글자를 보고했다**:

| 회차 | 보고된 깨진 문자열 |
|---|---|
| 1R S3 | `검검 중입니다` · `직접인` · `오이에` |
| 2R S3 | `점검 중근` · `크` · `자주거나` · `설시할` |

같은 파일의 같은 결함이면 같은 글자가 나와야 한다. 이미지에서 글자를 **읽어** 판정하는 방식
자체가 흔들린다는 신호다. 그래서 판정을 눈에서 뗐다 — 두 층으로:

1. **DOM 단언** (`p477-capture.capture.ts` 의 `MUST_RENDER`) — 캡처를 찍기 **전에** 화면의
   실제 텍스트가 `점검 중입니다` / `다시 시도` / `카카오톡 오픈채팅으로 문의하기` /
   `오픈채팅 코드` 를 포함하는지 단언한다. 캡처는 이 단언의 그림 버전이다.
2. **대조군 캡처** (`p477-control-login-390.png`) — 같은 실행·같은 브라우저·같은 폰트로 찍은
   **평소 화면**(로그인). 점검 화면의 한글이 정말 깨졌다면 이 대조군도 똑같이 깨져야 한다.
   대조군이 멀쩡한데 점검 화면만 깨졌다는 주장은 두 PNG 를 나란히 놓으면 성립하지 않는다.

⚠️ 판정자에게: 글자 깨짐을 주장하려면 **대조군 PNG 와 나란히** 비교하고, 두 장 모두에서
같은 문자열이 같은 방식으로 깨졌음을 보여라. 한 장만 보고 내린 판독은 위 표의 전례가 있다.
